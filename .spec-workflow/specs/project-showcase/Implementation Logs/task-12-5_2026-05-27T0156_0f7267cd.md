# Implementation Log: Task 12.5

**Summary:** Implemented scripts/verify-canary-regex-pair.mjs as a mechanical CI gate for the canary ↔ regex-list pair (src/__fixtures__/chokepoint-canary.ts ↔ src/lib/projects.test.ts). Mirrors Task 6.5's all-or-none + revert-shape pattern with the same BASE-derivation, testability seam, and CLI structure. Wired into ci.yml as step "Verify canary↔regex-list paired-merge" placed AFTER "Verify paired-merge for 6+7" and BEFORE "Build 1 (with drafts)". 8/8 self-tests pass via `node --test`.

**Timestamp:** 2026-05-27T01:56:51.143Z
**Log ID:** 0f7267cd-3b87-4718-91e4-195c39b65eea

---

## Statistics

- **Lines Added:** +290
- **Lines Removed:** -0
- **Files Changed:** 7
- **Net Change:** 290

## Files Modified
- .github/workflows/ci.yml

## Files Created
- scripts/verify-canary-regex-pair.mjs
- scripts/verify-canary-regex-pair.test.mjs
- scripts/__fixtures__/canary-pair/good-both.diff
- scripts/__fixtures__/canary-pair/good-neither.diff
- scripts/__fixtures__/canary-pair/bad-only-canary.diff
- scripts/__fixtures__/canary-pair/bad-only-test.diff

---

## Artifacts

### Functions

#### verifyCanaryRegexPair
- **Purpose:** Pure core check for the canary↔regex-list paired-merge contract; testability seam — accepts plain inputs so self-tests can bypass git
- **Location:** scripts/verify-canary-regex-pair.mjs
- **Signature:** verifyCanaryRegexPair({ changedFiles: string[], headSubject: string }) => { ok: boolean, exitCode: 0|1, diagnostic: string, kase: 'all-touched'|'none-touched'|'strict-subset'|'revert-shape-subset' }
- **Exported:** No

#### TRACKED_SET
- **Purpose:** Frozen array of the two paired files: chokepoint-canary.ts, projects.test.ts
- **Location:** scripts/verify-canary-regex-pair.mjs
- **Signature:** readonly string[2]
- **Exported:** No

#### resolveBase
- **Purpose:** Returns `origin/${GITHUB_BASE_REF}` on pull_request events; `origin/main` fallback for push events
- **Location:** scripts/verify-canary-regex-pair.mjs
- **Signature:** () => string
- **Exported:** No

#### gitChangedFiles
- **Purpose:** Runs `git diff --name-only ${base}...HEAD` and returns trimmed non-empty paths; warns + returns [] on git failure (non-PR skip)
- **Location:** scripts/verify-canary-regex-pair.mjs
- **Signature:** (base: string) => string[]
- **Exported:** No

#### gitHeadSubject
- **Purpose:** Runs `git log -1 --format=%s HEAD` for revert-shape detection
- **Location:** scripts/verify-canary-regex-pair.mjs
- **Signature:** () => string
- **Exported:** No

#### main
- **Purpose:** CLI entrypoint; runs only when invoked directly via process.argv[1] === fileURLToPath(import.meta.url) guard
- **Location:** scripts/verify-canary-regex-pair.mjs
- **Signature:** () => void (process.exit)
- **Exported:** No

### Integrations

#### Integration
- **Description:** CI gate wiring for canary↔regex-list pair
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** GHA sets GITHUB_BASE_REF on pull_request; verifier reads it (or falls back to origin/main on push) → git diff --name-only BASE...HEAD → tracked-pair intersection → exit 0/1 with diagnostic. Runs as ci.yml step "Verify canary↔regex-list paired-merge" between "Verify paired-merge for 6+7" and "Build 1 (with drafts)".

