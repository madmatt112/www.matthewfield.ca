---
id: 91cd1df5-cf59-4029-bc2d-a1828a90396f
taskId: "17"
specName: blog-core
version: 1
verdict: findings
timestamp: 2026-05-20T16:08:51.645Z
criticalCount: 0
warningCount: 0
infoCount: 1
---

# Task Review: Task 17 (v1)

**Spec:** blog-core
**Reviewed:** 2026-05-20T16:08:51.645Z
**Verdict:** findings

---

## Summary

Rule added per design: `no-restricted-imports` with importNames:['posts'] globally, per-file override turning rule off for src/lib/blog.ts and velite.config.ts. Sanity-checked: planted `import { posts } from "#site/content"` fires the helper-redirect error; revert returns to clean lint. One info finding: adding velite.config.ts to a `files` override block surfaced two pre-existing `any` types, which were tightened (Parameters<>/structural-typed visitor) to keep `pnpm lint` clean — scope expansion is contained and improves type safety.

## Findings

### INFO [hygiene]: velite.config.ts type tightening was a side effect of the override
- **File:** velite.config.ts
- **Issue:** Adding `velite.config.ts` to the per-file `files` override exposed it to typescript-eslint rules (notably `@typescript-eslint/no-explicit-any`) that previously were not surfacing errors for that file. Two `any` casts were narrowed (visit callback parameter typed structurally, countWordsFromMdast arg cast via `Parameters<>`) to keep lint green. The change is small and improves type safety, but it is a scope expansion beyond the lint-rule task — flagging for awareness in case a future maintainer wonders why this commit touched velite.config.ts.

