# Task Review Memory — Task 13 (site-foundation)
Last updated: 2026-04-17T16:58 (after v8 review)

## Cumulative Findings Summary

### Still Present (recurring/compounding in latest review)
- v6 (warning), v7 (info), v8 (info recurring) [info] Dark-mode test harness uses direct DOM manipulation + localStorage + DOMContentLoaded redundancy as preparation for task 16's next-themes integration. Task 16 hasn't shipped, so the localStorage/DOMContentLoaded paths remain dead code. Now explicitly accompanied by a follow-up procedure in spike-summary.txt:365-385 (re-audit Tests 4/5 immediately after task 16 ships) — visibility good, residual dead-code cost remains (e2e/tests/playground-isolation.test.ts:153).

### Addressed (present earlier, not found in latest)
- v5 [critical] Dark-mode test omitted required `color` computed-style assertion — Test 4 reads cs.color and asserts via expectRgbEqual.
- v5 (critical → escalated/clarified), v6 (warning), v7 (warning) [warning] R11 AC3 not directly verified via Button — Test 6 ('shadcn Button consumes re-established tokens via var() inline style', e2e/tests/playground-isolation.test.ts:351) now adds a Button-shaped fixture (src/app/(playground)/spike/page.tsx:67-77) with inline style backgroundColor=var(--primary), color=var(--primary-foreground); the before/after-dark equality on a value that differs in :root.dark proves the layered re-declaration wins, not just sympathetic-match. AC3 is now closed at the actual component path.
- v6 [warning] EXPECTED_* constants encoded Lightning-CSS serialization quirks as exact strings — replaced with numeric LabChannels tuples + parseLab + expectLabClose with toBeCloseTo tolerance.
- v6 [warning] AC2 plain-div test was tautological (inline styles win regardless) — Test 7 ('AC2 — site globals blocked at container boundary', e2e/tests/playground-isolation.test.ts:388) now anchors AC2 with a non-inline fixture (spike-ac2-inherit-target).
- v6 [info] EXPECTED_CONTAINER_COLOR comment claim — replaced with accurate explanation of `all: initial` → CanvasText keyword serialization.
- v6 [info] Test 5 didn't exercise cascade-layer hypothesis — Test 5 now reads inheritedBackground/foreground/primary AND asserts they remain at light values after dark-mode toggle.
- v5 [info] Comment overstated Turbopack/Webpack divergence risk — trimmed to one-line note.
- v5 [warning] Tailwind bg-blue-500 assertion didn't directly prove @layer playground delivery — spike-summary explicitly notes the observational limitation.
- v5 [warning] spike-summary recorded values that weren't asserted — offending lines removed.
- v6 (warning), v7 (info), v8 (addressed) [warning] Dev/production parity claim asserted not evidenced — spike-summary.txt:137-153 now provides a direct side-by-side computed-value comparison table (Webpack prod vs Turbopack dev for 15 fixture-property pairs), satisfying R11 AC5's literal "comparing computed style values for key properties" requirement.
- v7 [warning] Spike-discovered typography breakage means Button silently inherits Times New Roman — Test 2 (e2e/tests/playground-isolation.test.ts:236-239) now reads cs.fontFamily and asserts NOT toContain INTENDED_PLAYGROUND_FONT_FRAGMENT='ui-sans-serif' as the broken-state marker; the assertion will flip to failing when the playground.css fix lands, forcing a test update.
- v7 [warning] EXPECTED_INHERITED_FONT_FAMILY pinned Test 6 to Chromium-specific serif default — that constant is removed; Test 7 (the AC2 anchor) now uses negation-only against HOST_FONT_FAMILY_FRAGMENTS, surviving any platform-specific serif default.
- v7 [info hygiene] Per-test TEST 1-6 numbering in spike-summary didn't match Playwright reporter worker IDs — TEST N labels are dropped; per-test evidence now references tests by name; an explicit caveat at spike-summary.txt:13-16 explains that worker-ID prefixes in reporter output are NOT stable indices.

### New in Latest Review (v8)
- [info hygiene] Production reporter excerpt in spike-summary is stale relative to current test file — the production-build reporter block at lines 69-90 cites source lines that are off-by-one for every test except the first. The dev-build reporter block at lines 102-121 matches the current file exactly. A single line was added to the test file between the production and dev runs and was not re-captured. Implementation summary lists tasks.md as the only modified file, so this is also a documentation discrepancy. Re-run + replace the prod reporter block, or add a one-line offset note (e2e/spike-summary.txt:69).
- [info] Negation list in Test 2 will fail for the wrong reason after the typography fix — HOST_FONT_FAMILY_FRAGMENTS includes 'Helvetica' and 'Arial', but the intended playground stack in src/styles/playground.css:24-33 contains both ('Helvetica Neue' is a substring of 'Helvetica'; 'Arial' is in the stack). When playground.css is fixed, three not.toContain assertions will fail in Test 2 (Geist won't, but ui-sans-serif, Helvetica, AND Arial will), implying three regressions when the truth is one fix landing. Either narrow the negation list to fragments that cannot legitimately appear in the playground stack, or extend the flip-comment to enumerate which assertions to remove vs. invert (e2e/tests/playground-isolation.test.ts:93).

## Patterns & Themes
- The test file matured significantly across v5 → v8: v5 plugged correctness gaps (color assertion, dark-mode reads), v6 fixed serialization brittleness and tautology, v7's recommendations (Button-via-var, typography-as-assertion, negation-only AC2 anchor, direct value comparison table) are all implemented. v8 lands on a stable equilibrium where all substantive AC verifications exist.
- The remaining concerns are now overwhelmingly hygiene (artifact freshness, future-fix maintainability) rather than coverage gaps.
- One pattern worth noting: the test file's defensive-coding instinct (the dark-mode harness's localStorage + DOMContentLoaded redundancy for a ThemeProvider that doesn't exist yet) keeps generating soft findings. The recurring pattern reflects the task's prescience trying to insulate itself from task 16; the current review accepts this with the documented follow-up but it remains worth re-checking after task 16 lands.

## Guidance for Next Review
- **De-prioritize**: AC3 closure at the Button path (now solid via Test 6); typography-bug-as-assertion (encoded in Test 2); EXPECTED_INHERITED_FONT_FAMILY brittleness (constant removed, replaced with negation); dev/prod parity evidence (table now present); reporter ordering (stable & explained); lab() serialization (numeric tolerance throughout).
- **Focus**: (1) whether the spike-summary's prod reporter block has been refreshed (it's the one outstanding hygiene gap); (2) whether the negation-list overlap with the playground stack has been resolved or annotated; (3) whether task 16 has landed yet — if so, re-audit the dark-mode tests against the live next-themes hydration path per the procedure in spike-summary.txt:365-385.
- **Watch for**: scope creep into task 14's spike-results.md or task 15's @theme work — the summary recommendations sections are appropriate for a spike artifact but the actual recommendations should propagate, not stagnate here.
- **Verdict trajectory**: v8 is at "findings (info-only)" — there is no critical or warning-severity issue outstanding. A single more iteration to refresh the prod reporter block and clarify the negation list would plausibly land at a clean pass.
