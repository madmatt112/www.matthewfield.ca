# Implementation Log: Task 13

**Summary:** Exported `THEME_STORAGE_KEY = "theme"` constant from `src/components/layout/theme-provider.tsx` and wired it to the `next-themes` `storageKey` prop. The value matches the next-themes default (`'theme'`) — the provider previously passed no `storageKey` prop, so this preserves existing localStorage state for all users while giving E2E tests (task 23.1's contact-csp-axe.test.ts dark-theme axe pass) a single import-able source of truth, eliminating the silent-drift class where hardcoded `'theme'` in tests could diverge from a future custom key. The constant is placed before the spread of incoming props so callers retain the ability to override via the `storageKey` prop (none currently do — `src/app/layout.tsx` does not pass `storageKey`).

**Timestamp:** 2026-05-15T16:16:33.322Z
**Log ID:** e229017f-4787-41ed-b45a-2bd97796d324

---

## Statistics

- **Lines Added:** +7
- **Lines Removed:** -1
- **Files Changed:** 2
- **Net Change:** 6

## Files Modified
- src/components/layout/theme-provider.tsx
- .spec-workflow/specs/professional-profile/tasks.md

## Files Created
_No files created_

---

## Artifacts

### Components

#### ThemeProvider
- **Type:** React (client component)
- **Purpose:** Wraps next-themes ThemeProvider for the app shell; now passes an explicit, exported storageKey so tests and runtime use the same value.
- **Location:** src/components/layout/theme-provider.tsx
- **Props:** ComponentProps<typeof NextThemesProvider> (next-themes ThemeProvider props, spread). Internal default storageKey={THEME_STORAGE_KEY} is applied before the spread, so consumer-supplied storageKey wins.
- **Exports:** ThemeProvider, THEME_STORAGE_KEY

### Functions

#### THEME_STORAGE_KEY
- **Purpose:** Shared constant for the localStorage key used by next-themes; lets E2E tests (axe dark-theme pass) import the exact key the provider configures instead of hardcoding 'theme'.
- **Location:** src/components/layout/theme-provider.tsx:6
- **Signature:** export const THEME_STORAGE_KEY = "theme"
- **Exported:** No

