/.next
/.velite
/node_modules
/pnpm-lock.yaml
/public
/test-results
/playwright-report
/blob-report
/playwright/.cache

# Documentation and specs (authored by humans, not code)
/.spec-workflow
/research
/project-overview.md

# Local tooling (not project code)
/.claude

# Chokepoint canary fixtures — pinned code shapes the scanner regexes match
# verbatim; reformatting them silently breaks the canary tests.
/src/__fixtures__/chokepoint-canary.ts
/src/__fixtures__/content-chokepoint-canary.ts

# GitHub contributions audit fixture — the raw API payload, kept byte-identical
# so the seed in content/github-activity.yaml stays checkable against it.
# Reformatting it destroys the only thing it is for.
/scripts/__fixtures__/github-activity/
