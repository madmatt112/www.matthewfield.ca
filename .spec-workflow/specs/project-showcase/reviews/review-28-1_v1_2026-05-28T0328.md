---
id: ed144343-6454-4b12-bdc0-d5784885f676
taskId: "28.1"
specName: project-showcase
version: 1
verdict: findings
timestamp: 2026-05-28T03:28:28.012Z
criticalCount: 0
warningCount: 0
infoCount: 3
---

# Task Review: Task 28.1 (v1)

**Spec:** project-showcase
**Reviewed:** 2026-05-28T03:28:28.012Z
**Verdict:** findings

---

## Summary

All four 28.1 substeps verified green against on-disk artifacts (.next has zero fixture-placeholder hits, fixture-published-second present as control; .velite/projects.json reflects draft filtering; stderr_count==velite_draft_count==1). Restrictions honored: JSON-fragment Playwright citation, real .next glob, mechanical grep -c. One info note about pre-existing Case 13 failure (out of 28.1 scope).

## Findings

### INFO [hygiene]: Pre-existing Case 13-runtime failure unrelated to 28.1
- **File:** src/lib/projects.test.ts
- **Issue:** src/lib/projects.test.ts Case 13-runtime fails with expected '2025-12-01' got '2025-12-01T00:00:00.000Z' — Velite/Zod date coercion. Task 28.1 only required Cases 8 + 11 (both pass 4/4); Case 13 is out of scope for 28.1. Flag as a follow-up before tasks that depend on the projects.test.ts suite being fully green (e.g., a future CI gate).

### INFO: pnpm build does not invoke velite; stderr capture substrate worth pinning
- **Issue:** The implementation log correctly notes that `pnpm build` is `next build` only and that draft-warning stderr was captured from an explicit `pnpm exec velite build` invocation feeding the same .velite/projects.json consumed by e2e. Spec step 1 reads `PROJECTS_INCLUDE_DRAFTS=1 pnpm build` literally; the substrate substitution is sound but should be pinned in tasks.md or a CI script so future runs do not capture stderr from a different velite invocation than the one feeding the e2e suite.
- **Task Requirement:** 7.5

### INFO: Empty-state e2e spec skipped — expected, verified
- **Issue:** results[5]'renders the empty-state copy when no projects are published' status=skipped is intentional (guard skips when fixtures exist). Cited correctly in the log; no action.

