# Implementation Log: Task 24

**Summary:** Final integration gate. All six commands pass: pnpm typecheck, lint, format:check, test (vitest 30 passed), build, test:e2e (52 passed). Sitemap.xml at .next/server/app/sitemap.xml.body contains `<loc>https://matthewfield.ca/contact</loc>` (Req 5.6 verified).

Drove the gate to green via the following product/test fixes, each diagnosed as a root-cause regression rather than masking:

1. **Velite git-derived date** (per task body): committed content/profile.mdx (commit 6edd2ef "feat(profile): add profile.mdx content for Velite profile collection") so velite.config.ts profile collection's git-log transform produces a valid updatedAt. Before commit, `git log -1 --follow --format=%cI -- content/profile.mdx` returned empty, the transform's empty-result branch raised, .velite/profile.json was not written, and `pnpm build` failed with "Module not found: Can't resolve './profile.json'" in the .velite barrel.

2. **Test-locator ambiguity** (root cause: getByLabel substring-matching collided with ObfuscatedEmail's wrapper aria-label "Reveal Matthew's email address" which contains the literal "Email"). On /profile the form's "Email" input AND the obfuscated span both matched, raising Playwright strict-mode violations. The ObfuscatedEmail aria-label is design-pinned (Req 2.4) — fix applied in the test surface area (tasks 22/23/23.2) by adding `{ exact: true }` to the three field getByLabel calls in e2e/tests/contact-form.test.ts, contact-csp.test.ts, contact-reduced-motion.test.ts. Exact-match still resolves the form input (whose accessible name from `<Label htmlFor>` is exactly "Email") while excluding the substring-containing aria-label. This is not masking: introducing a duplicate exact-"Email" labeled element would still fail.

3. **Axe color-contrast in dark theme** (root cause: site-foundation defect surfaced by professional-profile task 23.1's axe gate). Two compounding issues:
   - tokens.css `--muted-foreground` in dark = oklch(0.708) = #a1a1a1 against the header backdrop (#3b3b3b from --muted/--secondary) = 4.33:1, below WCAG 2 AA 4.5:1. Bumped to oklch(0.74) with an inline comment explaining the deviation from upstream shadcn defaults.
   - body had no background-color set, so once axe walked the cascade up from the footer's text-muted-foreground links, the resolved background fell back to UA white #ffffff and contrast computed at 2.58:1 even in dark mode. Added `body { background-color: var(--background); color: var(--foreground); }` to src/styles/globals.css (commented as site-foundation surface needed for professional-profile task 23.1's axe gate). This is a real site-wide fix — the dark theme genuinely had no themed body background before.

4. **Pre-existing site-foundation tests stale-by-design** (root cause: task 18 replaced the /profile placeholder with the real RSC page; design.md §"Existing tests untouched" was inaccurate about smoke/landing/navigation):
   - e2e/tests/smoke.test.ts: title regex `/matthew-field\.ca/` updated to `/matthewfield\.ca/` to match the 0c8839e rename (drive-by; orthogonal to this spec).
   - e2e/tests/navigation.test.ts: desktop-nav loop and placeholder loop now branch on `item.href === '/profile'` — `/profile` no longer has H1 "Professional Profile" (it has the Velite-sourced headline "Infrastructure & platform engineer") and no longer renders the "under construction" copy. The branch asserts a level-1 heading exists (any name) for /profile while keeping the strict name match for the remaining placeholder routes; the placeholder-text test excludes /profile from its loop.
   - e2e/tests/landing.test.ts: hero-card loop similarly branches on `card.href === '/profile'`.

Sitemap verification (Req 5.6): one-line shell check confirmed:
  `grep -c "loc>https://matthewfield.ca/contact</loc" .next/server/app/sitemap.xml.body` → 1

Sitemap also contains /profile, /projects, /contributions, /blog, /resources, /playground, /about, /colophon, /now, /sitemap, /slashes (12 URLs).

Headshot responsive viewport check (Req 1.5): manual verification surface per task body. profile.mdx in this commit has no `headshot` frontmatter field (Velite s.image() is optional). The `<section className="flex flex-col items-start gap-6 md:flex-row md:items-center md:gap-10">` wrapper in src/app/(site)/profile/page.tsx implements the stacks-below-md / side-by-side-at-md layout per design — verified by code inspection. The headshot itself is gated behind `profile.headshot ?` so the layout still composes correctly without it (only the headline/meta column renders); when a headshot is added in a follow-up content commit, the same flex container will surface it side-by-side at md+ widths. Reviewer can validate the responsive shape manually via dev tools at 320/768/1280 once a headshot asset lands.

CSP gate (task-body requirement): e2e/tests/csp.test.ts ran alongside the new tests and passed (3 cases). form-action 'self' header assertion in contact-csp.test.ts also passes for both /profile and /contact.

Total E2E wall time: ~10s (well under the multi-minute budget anticipated by the parent agent — the wrapper script's mock+dev-server boot is the dominant cost; the test run itself is fast). Total gate wall time across the five non-E2E commands: ~21s.

Files modified (non-test): src/styles/globals.css (body bg+fg), src/styles/tokens.css (dark muted-foreground lightness).
Files modified (test selectors, in their owning tasks' surface areas): e2e/tests/contact-form.test.ts, contact-csp.test.ts, contact-reduced-motion.test.ts.
Files modified (site-foundation drive-by, stale assumptions): e2e/tests/smoke.test.ts, navigation.test.ts, landing.test.ts.
New commit: 6edd2ef on main (content/profile.mdx tracked — task-body-permitted).

Reviewer notes: the token bump (0.708 → 0.74) and globals.css body rule are minimal, principled fixes that close the dark-theme contrast hole the axe gate is meant to catch — reverting either reproduces the failure cleanly. The test-locator { exact: true } additions are tight: they specifically reject substring matches against neighbouring aria-labels without weakening the assertion. The site-foundation test-file edits assume /profile's new H1 is the headline — if a future change re-introduces the literal "Professional Profile" string as the H1, the branch is harmless (the relaxed assertion still passes). Did NOT mark task 24 [x] — leaving for dashboard review per the spec-workflow loop convention.

**Timestamp:** 2026-05-15T20:12:09.343Z
**Log ID:** 848a02d4-74d3-4326-83e3-48a45967fbe2

---

## Statistics

- **Lines Added:** +53
- **Lines Removed:** -19
- **Files Changed:** 9
- **Net Change:** 34

## Files Modified
- src/styles/globals.css
- src/styles/tokens.css
- e2e/tests/contact-form.test.ts
- e2e/tests/contact-csp.test.ts
- e2e/tests/contact-reduced-motion.test.ts
- e2e/tests/smoke.test.ts
- e2e/tests/navigation.test.ts
- e2e/tests/landing.test.ts
- .spec-workflow/specs/professional-profile/tasks.md

## Files Created
_No files created_

---

## Artifacts

### Functions

#### sitemap presence check (one-liner)
- **Purpose:** Verify Req 5.6 — that the build-emitted sitemap enumerates /contact. Run after pnpm build.
- **Location:** shell (ad-hoc; not committed)
- **Signature:** grep -c 'loc>https://matthewfield.ca/contact</loc' .next/server/app/sitemap.xml.body  # → 1
- **Exported:** No

### Integrations

#### Integration
- **Description:** Local quality gate composite: typecheck → lint → format:check → vitest → next build → playwright e2e. All six exit 0 against current main. The build step depends on content/profile.mdx being tracked (so velite.config.ts profile transform's git-log execFileSync returns a non-empty %cI date) — committed in this task per task-body permission.
- **Frontend Component:** n/a (verification pass)
- **Backend Endpoint:** n/a (verification pass)
- **Data Flow:** git working tree → tsc --noEmit → eslint . → prettier --check . → vitest run → next build (which runs velite postinstall + builds .next/server/app/sitemap.xml.body containing <loc>https://matthewfield.ca/contact</loc>) → scripts/run-e2e.mjs (boots mock-resend + Next dev on 3013, runs Playwright)

#### Integration
- **Description:** Body theming integration: globals.css now applies var(--background)/var(--foreground) to <body>, so the next-themes class toggle on <html.dark> cascades through to the page background. Closes the axe-core contrast computation hole where body fell back to UA white and dark-mode contrast ratios were measured against #ffffff.
- **Frontend Component:** src/components/layout/theme-provider.tsx (unchanged) → src/styles/globals.css (body rule added) → src/styles/tokens.css (--muted-foreground bumped in .dark)
- **Backend Endpoint:** n/a
- **Data Flow:** next-themes sets html.dark → tokens.css .dark { --background: oklch(0.145) } → globals.css body { background-color: var(--background) } → axe-core color-contrast walks DOM cascade and resolves the real dark background → contrast checks pass for header nav + footer nav links

