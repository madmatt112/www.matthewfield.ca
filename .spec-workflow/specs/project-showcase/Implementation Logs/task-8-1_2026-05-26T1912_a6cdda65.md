# Implementation Log: Task 8.1

**Summary:** Added the `projects` Velite collection schema (Component 1 v4 verbatim) and the sibling `linkSchema` helper in `velite.config.ts`. Schema uses `.strict()`, two-stage URL validation (`.url()` + `.refine()` for http/https protocol), and a custom `errorMap` on the `kind` enum that emits the exact Req 5.1 rejection string. Transform is identity-only (`.transform((d) => d)`); collection is NOT yet registered in `defineConfig` (Task 8.4 will register after 8.2/8.3/8.4 complete the transform pipeline). `pnpm typecheck` passes; `pnpm velite build` finishes cleanly.

**Timestamp:** 2026-05-26T19:12:17.536Z
**Log ID:** a6cdda65-6450-4606-89b0-9015fc05ba24

---

## Statistics

- **Lines Added:** +67
- **Lines Removed:** -11
- **Files Changed:** 2
- **Net Change:** 56

## Files Modified
- velite.config.ts
- .spec-workflow/specs/project-showcase/tasks.md

## Files Created
_No files created_

---

## Artifacts

### Functions

#### linkSchema
- **Purpose:** Per-entry validation for the projects.links array. Closed enum on `kind` with exact Req 5.1 error-message contract; two-stage URL validation (Zod .url() + .refine() new URL() protocol check restricting to http:/https:).
- **Location:** velite.config.ts:246-275
- **Signature:** const linkSchema = s.object({ kind?: s.enum(["demo","repo","docs","package","writeup"]).optional(), label: s.string().min(1).max(60), url: s.string().url().refine(protocolIsHttp) }).strict()
- **Exported:** No

#### projects (Velite collection)
- **Purpose:** Velite collection for project-showcase MDX content. Schema per Component 1 v4: title (1-120), description (50-160), summary (30-140), date (isodate), cover (image), coverAlt (1-250), tags (kebab-slug regex, max 8, default []), status (enum [active,archived,concept] default active), ogImage (optional image), updated (optional isodate), draft (boolean default false), slug (path), featured (boolean default false), links (array of linkSchema, max 6, optional), body (mdx). `.strict()` rejects unknown frontmatter keys. Transform is identity placeholder — Tasks 8.2/8.3/8.4 extend. NOT registered in defineConfig yet (collections list unchanged: `{ pages, profile, posts }`).
- **Location:** velite.config.ts:281-303
- **Signature:** const projects = defineCollection({ name: "Project", pattern: "projects/*.mdx", schema: s.object({...}).strict().transform((d) => d) })
- **Exported:** No

### Integrations

#### Integration
- **Description:** Custom errorMap on linkSchema.kind intercepts `invalid_enum_value` issues and emits the verbatim Req 5.1 string: "links[<i>].kind '<bad-value>' is not in {demo,repo,docs,package,writeup}; omit 'kind' for a label-only entry with no icon." Falls through to ctx.defaultError for any other issue code.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

