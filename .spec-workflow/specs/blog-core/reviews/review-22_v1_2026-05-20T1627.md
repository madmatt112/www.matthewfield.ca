---
id: ed77a2b2-2790-4721-9bef-bb375b0df99d
taskId: "22"
specName: blog-core
version: 1
verdict: findings
timestamp: 2026-05-20T16:27:26.630Z
criticalCount: 0
warningCount: 0
infoCount: 2
---

# Task Review: Task 22 (v1)

**Spec:** blog-core
**Reviewed:** 2026-05-20T16:27:26.630Z
**Verdict:** findings

---

## Summary

Implementation meets all task requirements (6.0, 6.5, 7.5, 7.6, 7.12, 8.0). All 74 tests pass. One info-level note on the end-to-end identity assertion's frontmatter-strip approach (acceptable substitute for `meta.content` since `body` is compiled MDX and `bodyHtml` is HTML — neither is re-parsable with `remark-parse`).

## Findings

### INFO: End-to-end identity reads .mdx and strips frontmatter via regex
- **File:** /home/mcf/repo/matthew-field.ca/src/lib/blog.test.ts:207
- **Issue:** The task note cautions against naively reading raw .mdx (which would include frontmatter and produce a different tree). The test strips the leading `^---\n...\n---\n` block before parsing, reproducing the input Velite's transform sees (`meta.content`). Since neither `body` (compiled MDX JS) nor `bodyHtml` (HTML) is re-parsable by `remark-parse + remark-gfm`, reading the raw file and stripping frontmatter is the closest faithful reproduction available. Acceptable as-is for current fixtures (none use --- inside body text).
- **Task Requirement:** 6.5

### INFO [hygiene]: globalSetup runs `pnpm velite build` on every `vitest run`
- **File:** /home/mcf/repo/matthew-field.ca/vitest.global-setup.ts
- **Issue:** Adds ~800ms per test invocation. Acceptable per the task pre-requisite (`Vitest's globalSetup runs velite build before tests`). A future optimization could skip when `.velite/posts.json` is newer than all `content/posts/*.mdx` files, but conditional logic adds complexity; the simple always-build path is preferred until rebuild cost becomes a measurable pain point.

