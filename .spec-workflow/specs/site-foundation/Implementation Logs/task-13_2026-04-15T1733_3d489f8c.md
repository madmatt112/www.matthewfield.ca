# Implementation Log: Task 13

**Summary:** Added Playwright verification tests for the CSS isolation spike that assert plain-div style isolation, utility-class delivery to shadcn Button, Tailwind utilities resolving inside @layer playground, and dark-mode token isolation; recorded actual production-build computed values in e2e/spike-summary.txt for task 14 to reference.

**Timestamp:** 2026-04-15T17:33:07.999Z
**Log ID:** 3d489f8c-5e79-4035-ba6f-e276dd81d02f

---

## Statistics

- **Lines Added:** +290
- **Lines Removed:** -1
- **Files Changed:** 3
- **Net Change:** 289

## Files Modified
- /home/mcf/repo/matthew-field.ca/.spec-workflow/specs/site-foundation/tasks.md

## Files Created
- /home/mcf/repo/matthew-field.ca/e2e/tests/playground-isolation.test.ts
- /home/mcf/repo/matthew-field.ca/e2e/spike-summary.txt

---

## Artifacts

### Functions

#### readComputed
- **Purpose:** Helper that locates an element by data-testid via Playwright's getByTestId locator and runs a serialized reader function inside the page context to return computed-style values
- **Location:** e2e/tests/playground-isolation.test.ts:48
- **Signature:** <T>(page: Page, testId: string, reader: (el: HTMLElement) => T) => Promise<T>
- **Exported:** No

### Integrations

#### Integration
- **Description:** Production-build Playwright suite that drives /spike (the (playground) route group fixture from tasks 11-12) and asserts CSS isolation properties of .playground-container against the shadcn/ui token re-declarations in src/styles/playground.css. The test reads actual computed values via page.evaluate(() => getComputedStyle(...)) and compares them to lab()-serialized expectations because Lightning CSS rewrites oklch() in the production output. The dark-mode test toggles document.documentElement.classList.add('dark') and re-reads the container's --background/--foreground/--primary/--primary-foreground custom properties to confirm the @layer playground re-declarations win over inherited host tokens. Findings (including the deliberately-deferred Button background/color check, which depends on task 15's @theme block) are mirrored to e2e/spike-summary.txt so task 14 can derive the graduated outcome without re-running the suite.
- **Frontend Component:** SpikePage + SpikeOverlays (src/app/(playground)/spike/page.tsx, src/app/(playground)/spike/spike-overlays.tsx) inside PlaygroundLayout (src/app/(playground)/layout.tsx)
- **Backend Endpoint:** n/a
- **Data Flow:** PORT=3100 pnpm build && PORT=3100 pnpm test:e2e -> Playwright webServer launches `next start --port 3100` -> chromium navigates to /spike -> PlaygroundLayout wraps fixture in .playground-container which imports playground.css -> page.evaluate() reads getComputedStyle on data-testid elements and the container -> assertions compare against lab()-serialized expected constants -> all 5 tests pass -> spike-summary.txt records the empirical readouts for task 14 to consume

