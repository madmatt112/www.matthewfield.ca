# Implementation Log: Task 27

**Summary:** Extended src/lib/blog.test.ts with Vitest cases covering isHiddenFromLists(), getVisiblePublishedPosts(), getSeriesGroups(), getRelatedPosts() (algorithm correctness — 3:1 tag-vs-category weighting, score=0 filter, limit honored, deterministic date+slug tiebreak, same-series exclusion at ≥2 visible members, series-of-1 dead-zone closure per Req 4.2, unknown-slug returns []), extractToc() (h2/h3 parse, document order, duplicate-heading collision-suffix surfaced from rehype-slug, h4+ ignored, fewer-than-2-headings → []), and a Velite-emitted-slug parity test asserting post.slug === derivePostSlug(POST_FILE_PATH, post) for every post in the built manifest. To support fixture-driven algorithm testing without inlining mock posts, refactored getRelatedPosts() to extract relatedPostsFromList(query, candidates, limit) and exposed it via the existing __testing handle. Also fixed a regression in velite.config.ts where derivePostSlug was being called with the velite-processed data (whose .slug field had been populated by s.path() with the "posts/" prefix), causing every emitted slug to be "posts/<basename>" instead of "<basename>" — pass { slug: undefined } so derivePostSlug falls through to its basename branch (mirrors the pages-collection pattern). This regression broke the two pre-existing fixture-lookup tests (fixture-code neighbors, fixture-reading-time identity) as well as every downstream URL/slug consumer; the new Velite-emitted-slug parity test now guards against it. All 47 cases in src/lib/blog.test.ts pass; tsc --noEmit is clean. Pre-existing lint error on blog.test.ts (#site/content import restriction from a chokepoint guard) was not introduced by this task — the existing test file already had it before Task 27; modifying eslint.config.mjs to exempt the test file was out of scope.

**Timestamp:** 2026-05-22T16:50:52.423Z
**Log ID:** 1bb1e880-1c00-47e9-b026-337816810c58

---

## Statistics

- **Lines Added:** +290
- **Lines Removed:** -35
- **Files Changed:** 4
- **Net Change:** 255

## Files Modified
- /home/mcf/repo/matthew-field.ca/src/lib/blog.test.ts
- /home/mcf/repo/matthew-field.ca/src/lib/blog.ts
- /home/mcf/repo/matthew-field.ca/velite.config.ts
- /home/mcf/repo/matthew-field.ca/.spec-workflow/specs/blog-enhanced/tasks.md

## Files Created
_No files created_

---

## Artifacts

### Functions

#### relatedPostsFromList
- **Purpose:** Internal helper extracted from getRelatedPosts() — takes a query Post, a candidate Post[] (mirroring getVisiblePublishedPosts() output, including the query itself for the series-count loop), and a limit; returns RelatedPostMeta[] scored under the 3:1 tag-vs-category weighting with date-desc/slug-asc tiebreak. Exposed via the __testing object so fixture-driven algorithm tests can exercise scoring/ordering/series-exclusion paths without mutating module-level state.
- **Location:** /home/mcf/repo/matthew-field.ca/src/lib/blog.ts (relatedPostsFromList, exported via __testing alongside the existing neighbors helper)
- **Signature:** (query: Post, candidates: Post[], limit: number) => RelatedPostMeta[]
- **Exported:** No

### Integrations

#### Integration
- **Description:** velite.config.ts posts.transform now calls derivePostSlug(meta.path, { slug: undefined }) instead of derivePostSlug(meta.path, data). Velite's s.path() schema populates data.slug with the velite-root-relative path (`posts/<basename>`) before the transform runs, which caused derivePostSlug's `if (frontmatter.slug) return frontmatter.slug` branch to short-circuit and return the prefixed slug. Passing an explicit undefined slug forces the basename branch, restoring parity with the pages-collection pattern and with derivePostSlug.test.mjs case (a).
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

