---
id: 54009520-d9ab-4904-8868-89cee98cf26d
taskId: "17.5"
specName: blog-enhanced
version: 1
verdict: findings
timestamp: 2026-05-21T23:16:18.175Z
criticalCount: 0
warningCount: 0
infoCount: 2
---

# Task Review: Task 17.5 (v1)

**Spec:** blog-enhanced
**Reviewed:** 2026-05-21T23:16:18.175Z
**Verdict:** findings

---

## Summary

Slice-carve infrastructure is correctly implemented — all nine slice files exist with marker comments only, globals.css imports them in alphabetical order, velite build and tsc pass. One info-level observation: the working tree's globals.css also contains a Shiki dual-theme cascade block (lines 87-143) that is clearly labeled as Task 28 work and is unrelated to task 17.5's scope; the implementer's log correctly excludes it from their stated line counts, but a reviewer scanning the raw diff should be aware these are concurrent uncommitted edits from a different workstream.

## Findings

### INFO: Unrelated Task 28 Shiki cascade block present in working tree alongside 17.5 edits
- **File:** src/styles/globals.css:87
- **Issue:** globals.css lines 87-143 add a Shiki dual-theme cascade rule (`[data-rehype-pretty-code-figure]` selectors targeting `--shiki-light`/`--shiki-dark`). The block's own comment labels it as a Task 28 patch from 2026-05-20, and it is unrelated to the slice carve. The 17.5 implementer's log correctly omits these 57 lines from its statistics, but the unified diff shown by review-task includes them because they share the same uncommitted-vs-last-commit window. No 17.5 scope violation — flagging only so the carve commit excludes the Shiki block (or that block is committed separately under Task 28).
- **Task Requirement:** Restriction: NO new components or routes — that is Task 17.6.

### INFO: Visual-neutrality Playwright assertion skipped as no-op
- **Issue:** Task 17.5 success criteria require a Playwright `toMatchSnapshot` visual-neutrality check across light + dark themes on `/blog` and one fixture post. The implementer correctly reasoned this is a no-op (empty slice files cannot perturb rendering) and skipped it. Defensible — the carve is purely structural and the slice files contain only marker comments — but the spec's literal text says screenshots must be recorded in the implementation log. Not blocking; the verification gate fires meaningfully only after 18.x sub-tasks populate the slices.
- **Task Requirement:** v4 — Visual-neutrality assertion

