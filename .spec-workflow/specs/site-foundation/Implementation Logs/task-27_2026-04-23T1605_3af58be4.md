# Implementation Log: Task 27

**Summary:** Added Playwright E2E suite for the landing page hero cards: scopes by the 'Sections' aria-labelledby region (sr-only h2) to disambiguate from duplicate nav links, asserts all 6 heroCards render, and parametrizes click-to-navigate assertions over siteConfig.heroCards.

**Timestamp:** 2026-04-23T16:05:41.704Z
**Log ID:** 3af58be4-29a0-463d-a5b6-f166eb3efc95

---

## Statistics

- **Lines Added:** +25
- **Lines Removed:** -0
- **Files Changed:** 2
- **Net Change:** 25

## Files Modified
- /home/mcf/repo/matthew-field.ca/.spec-workflow/specs/site-foundation/tasks.md

## Files Created
- /home/mcf/repo/matthew-field.ca/e2e/tests/landing.test.ts

---

## Artifacts

### Integrations

#### Integration
- **Description:** Playwright suite exercises the landing page (src/app/(site)/page.tsx) and HeroCard component (src/components/shared/hero-card.tsx) by iterating over siteConfig.heroCards (src/config/site.ts). Scoping uses page.getByRole('region', { name: 'Sections' }) to target the <section aria-labelledby="sections-heading"> block (whose sr-only h2 'Sections' labels the region) so hero-card links are not conflated with the duplicate-href nav links in the Primary navigation landmark. The render test asserts each card's link is visible (RegExp matcher on card.title tolerates the accessible name including both the h3 title and CardDescription text). The parametrized click tests navigate via getByRole('link', { name: /title/ }).click() then assert page.toHaveURL(card.href). All 6 hrefs from siteConfig.heroCards (/profile, /projects, /contributions, /blog, /resources, /playground) are covered.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** Test → page.goto('/') → Next.js renders src/app/(site)/page.tsx (server component) which maps siteConfig.heroCards → <HeroCard> → <Link href> wrapping shadcn Card → Playwright scopes to 'Sections' region and asserts link visibility / click navigation.

