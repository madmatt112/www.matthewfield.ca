# Implementation Log: Task 12

**Summary:** Added Vitest unit tests for src/app/api/contact/route.ts at src/app/api/contact/route.test.ts. The suite mocks @/lib/mail via vi.mock to stub sendContactEmail plus the TimeoutError/ResendError classes. It covers every branch of the Req 3.5 a–g pipeline: oversize body (>32KB) → 413; origin/referer accepted-set across the four host classes (matching production NEXT_PUBLIC_SITE_URL host, foreign-host 403, *.vercel.app wildcard, localhost, both-absent fallback); malformed JSON → 400; non-plain-object bodies (null, [], "string", 42) → 400; honeypot (url_secondary populated) → 200 silent with sendContactEmail NOT called; per-field zod failures (name empty, email invalid, message too short) → 400 with matching errors key; happy path → 200 + sendContactEmail called once with normalized source; unknown source values normalized to undefined via z.enum().catch(undefined); testId forwarded when string AND undefined when non-string (e.g. number) — taken off the raw parsed body, not the .strip()'d zod output; vendor-error mapping: TimeoutError → 503 with Retry-After: 60 header (header asserted explicitly) and ResendError → 502. Logging-discipline assertion: console.warn is called exactly once on each vendor-error branch with the literal string 'resend_timeout' / 'resend_4xx_5xx' and the aggregated console output across warn/error/log/info contains no substring of body.name/email/message/source. NEXT_PUBLIC_SITE_URL pinned via vi.stubEnv to 'https://matthewfield.ca' so the production-origin and foreign-origin classes are deterministic. No dev server required; route handler is invoked by passing a synthetic `new Request(...)` directly to POST. 21 test cases pass; full vitest suite (30 tests across 3 files) passes; typecheck and lint clean. No deviations from the task body — every required case is present.

**Timestamp:** 2026-05-15T16:12:21.457Z
**Log ID:** 307907e7-5d69-41a1-9f20-d9bb15c74774

---

## Statistics

- **Lines Added:** +267
- **Lines Removed:** -0
- **Files Changed:** 2
- **Net Change:** 267

## Files Modified
- /home/mcf/repo/matthew-field.ca/.spec-workflow/specs/professional-profile/tasks.md

## Files Created
- /home/mcf/repo/matthew-field.ca/src/app/api/contact/route.test.ts

---

## Artifacts

### Functions

#### makeRequest
- **Purpose:** Test helper: build a synthetic POST Request to /api/contact with sensible defaults (https origin matching the pinned NEXT_PUBLIC_SITE_URL, JSON content-type). Accepts an optional rawBody override for the oversize/malformed/non-plain-object cases that need to bypass JSON.stringify.
- **Location:** src/app/api/contact/route.test.ts:54
- **Exported:** No

#### assertNoUserInputLogged
- **Purpose:** Test helper: aggregate console.warn/error/log/info call arguments across the spies and assert that no logged string contains the request body's name/email/message/source. Enforces Req 3.10 logging discipline across the vendor-error branches.
- **Location:** src/app/api/contact/route.test.ts:40
- **Exported:** No

### Integrations

#### Integration
- **Description:** undefined
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

