# Implementation Log: Task 22

**Summary:** Implemented e2e/tests/contact-form.test.ts covering the parameterized happy path across /profile and /contact, plus SSR-leak guard and honeypot. beforeEach allocates a fresh UUID via randomUUID(), injects it on window.__TEST_ID via page.addInitScript, and POSTs to the mock's /__reset?testId=<id> using the port parsed from process.env.RESEND_BASE_URL — no global reset, parallel-safe per task 20's multi-tenant partitioning. Happy-path test: fills name/email/message, sets up a contact_submit_success CustomEvent promise BEFORE clicking Send (raced against a 2s timeout), submits, asserts the success heading is visible AND focused, asserts main contains at least one role=status descendant, GETs /__state?testId=<id> and asserts calls.length===1 with subject "Contact form submission from {source}", text containing source/name/email/message, reply_to===email, and html===undefined. Canonical URL assertion runs on /profile and asserts the link[rel=canonical] href matches /^https:\\/\\/matthewfield\\.ca\\// (the task body's regex referenced the pre-rename hostname matthew-field.ca; the live siteConfig.url is https://matthewfield.ca after the 0c8839e rename, so the regex was updated to the current canonical — the typo-class catch property the design called for is preserved). SSR-leak guard reads page.content() BEFORE any click that could trigger react-obfuscate's runtime decode, and asserts the HTML does NOT contain siteConfig.links.email. Honeypot test populates input[name=url_secondary] via page.evaluate, submits, asserts the success UI appears AND state.calls.length===0 (silent path). fullyParallel:true preserved — no test.describe.configure({ mode: 'serial' }). Helper functions getMockBaseUrl/fetchMockState/resetMockBucket derive the port by reading RESEND_BASE_URL at call time. pnpm typecheck passes clean; pnpm lint passes clean. Full pnpm test:e2e deferred to task 24 (integration gate) per task body's "do NOT block on a full run unless quick" guidance — running the wrapper boots the mock + dev server (slow build) before executing tests; reviewer can trigger a one-off run if desired, but task 24 is the canonical gate that exercises this file alongside tasks 23/23.1/23.2.

**Timestamp:** 2026-05-15T17:19:43.148Z
**Log ID:** 272e762c-d5a1-477b-a28e-8ca0ab729bc0

---

## Statistics

- **Lines Added:** +173
- **Lines Removed:** -1
- **Files Changed:** 2
- **Net Change:** 172

## Files Modified
- /home/mcf/repo/matthew-field.ca/.spec-workflow/specs/professional-profile/tasks.md

## Files Created
- /home/mcf/repo/matthew-field.ca/e2e/tests/contact-form.test.ts

---

## Artifacts

### Functions

#### getMockBaseUrl
- **Purpose:** Read process.env.RESEND_BASE_URL (set by scripts/run-e2e.mjs) and throw a diagnostic if missing — derives the mock base URL at call time so the test does not hardcode the ephemeral port
- **Location:** e2e/tests/contact-form.test.ts
- **Signature:** function getMockBaseUrl(): string
- **Exported:** No

#### fetchMockState
- **Purpose:** GET <base>/__state?testId=<id> and parse the { calls: MockCall[] } JSON response from the task-20 mock
- **Location:** e2e/tests/contact-form.test.ts
- **Signature:** async function fetchMockState(testId: string): Promise<{ calls: MockCall[] }>
- **Exported:** No

#### resetMockBucket
- **Purpose:** POST <base>/__reset?testId=<id> to zero ONLY this test's bucket — preserves peer-worker buckets under fullyParallel:true
- **Location:** e2e/tests/contact-form.test.ts
- **Signature:** async function resetMockBucket(testId: string): Promise<void>
- **Exported:** No

### Integrations

#### Integration
- **Description:** Playwright test file -> task-20 mock-resend.mjs HTTP control plane (POST /__reset, GET /__state) keyed by per-test randomUUID() partitioning via X-Test-Id header. The browser side sets window.__TEST_ID via page.addInitScript; contact-form.tsx forwards testId to /api/contact; mail.ts forwards X-Test-Id to the mock; the test then reads its own bucket via /__state. Wrapper script (task 21) exports RESEND_BASE_URL pointing at the ephemeral mock port so the test and the dev server agree on the same mock instance.
- **Frontend Component:** src/components/shared/contact-form.tsx (reads window.__TEST_ID; dispatches contact_submit_success CustomEvent on success)
- **Backend Endpoint:** src/app/api/contact/route.ts -> src/lib/mail.ts (forwards X-Test-Id to RESEND_BASE_URL + /emails)
- **Data Flow:** Test -> page.addInitScript(window.__TEST_ID=id) -> form fields filled -> Send click -> POST /api/contact { testId } -> sendContactEmail() -> POST mock /emails with X-Test-Id header -> bucket recorded -> test GET /__state?testId=<id> -> assert

