# Implementation Log: Task 33

**Summary:** Implemented Playwright `blog-toc.test.ts` proving three-source heading-ID parity across two build axes. Source A: rendered DOM (`article h2[id], article h3[id]`). Source B: `extractToc(post)` from `@/lib/blog`. Source C: `post.bodyHtml` (`s.markdown()` output) parsed via `node-html-parser`. Build 1 axis covers `/blog/fixture-toc` + `/blog/fixture-code` (skips when Pagefind index IS present). Build 2 axis covers `/blog/fixture-search` plus a source-level guardrail against `fixture-toc`'s duplicate-`## Setup` pair (skips when Pagefind index is NOT present). Each axis asserts ordered equality across all three sources so a `rehype-slug` regression in either `s.mdx()` or `s.markdown()` fails loudly (closes Req 7.4 v4 parity gap). Uses real `extractToc` and the real Velite `posts.json`, never reimplementations.

**Timestamp:** 2026-05-23T03:02:44.300Z
**Log ID:** abfc174d-926a-4323-ac69-38733086451f

---

## Statistics

- **Lines Added:** +144
- **Lines Removed:** -1
- **Files Changed:** 2
- **Net Change:** 143

## Files Modified
- .spec-workflow/specs/blog-enhanced/tasks.md

## Files Created
- e2e/tests/blog-toc.test.ts

---

## Artifacts

### Functions

#### readRenderedHeadingIds
- **Purpose:** Reads h2[id]/h3[id] heading IDs from the rendered <article> in document order via page.evaluate.
- **Location:** e2e/tests/blog-toc.test.ts
- **Signature:** (page: Page) => Promise<string[]>
- **Exported:** No

#### readBodyHtmlHeadingIds
- **Purpose:** Parses post.bodyHtml (s.markdown() output) with node-html-parser and extracts ordered h2/h3 IDs.
- **Location:** e2e/tests/blog-toc.test.ts
- **Signature:** (bodyHtml: string) => string[]
- **Exported:** No

#### readExtractTocIds
- **Purpose:** Wraps the real `extractToc` from @/lib/blog and returns the ordered ID array.
- **Location:** e2e/tests/blog-toc.test.ts
- **Signature:** (post: Post) => string[]
- **Exported:** No

#### assertThreeWayParity
- **Purpose:** Navigates to /blog/<slug> and asserts ordered equality across all three heading-ID sources.
- **Location:** e2e/tests/blog-toc.test.ts
- **Signature:** (page: Page, slug: string) => Promise<void>
- **Exported:** No

### Integrations

#### Integration
- **Description:** undefined
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

