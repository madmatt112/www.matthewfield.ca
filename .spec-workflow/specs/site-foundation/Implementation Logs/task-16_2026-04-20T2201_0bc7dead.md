# Implementation Log: Task 16

**Summary:** Create root layout with font loading, ThemeProvider, base metadata, and flip stylesheet import to @/styles/globals.css. Rewrites src/app/layout.tsx as a server component: loads Geist/Geist_Mono via next/font/google with CSS variable names --font-sans and --font-mono (dropping the previous --font-geist-* names so the values feed Tailwind's default font-sans/font-mono utilities directly), adds suppressHydrationWarning to html for next-themes, imports @/styles/globals.css (the task-15-authored file with the active @theme inline block) and deletes the stale src/app/globals.css CNA scaffold, exports metadata with title default/template pair and openGraph image, and wraps body children in a new ThemeProvider client component that passes attribute=class/defaultTheme=system/enableSystem/disableTransitionOnChange through to next-themes. Performs the task-15 carry-over R9 AC3 verification empirically in a production build: built CSS shows `.bg-primary{background-color:var(--primary)}` with `:root{--primary:#171717;--primary-foreground:#fafafa;...}` and `.dark{--primary:#e5e5e5;--primary-foreground:#171717;...}`, and a live Playwright check against `next start` port 3001 on a temporary scratch page confirmed the Button's computed background-color swaps from lab(7.78201%) to lab(90.952%) and its color from lab(98.26%) to lab(7.78201%) when `.dark` is toggled on `<html>` (after the shadcn Button's transition-all settled — the initial oklab intermediate value was the animation mid-frame). The temp scratch page and .playwright-mcp artifact directory were removed after verification. metadataBase was intentionally not set; the build-time warning ("using http://localhost:3000") is deferred to task 18/20 where siteConfig.url becomes the canonical source.

**Timestamp:** 2026-04-20T22:01:47.092Z
**Log ID:** 0bc7dead-031c-43a8-bdb7-b7e0646381f8

---

## Statistics

- **Lines Added:** +38
- **Lines Removed:** -30
- **Files Changed:** 5
- **Net Change:** 8

## Files Modified
- src/app/layout.tsx
- package.json
- pnpm-lock.yaml
- .spec-workflow/specs/site-foundation/tasks.md

## Files Created
- src/components/layout/theme-provider.tsx

---

## Artifacts

### Components

#### ThemeProvider
- **Type:** React (client)
- **Purpose:** Thin pass-through wrapper around next-themes ThemeProvider so the root layout (a server component) can mount it without crossing the client boundary itself. Receives attribute=class/defaultTheme=system/enableSystem/disableTransitionOnChange from src/app/layout.tsx.
- **Location:** src/components/layout/theme-provider.tsx
- **Props:** ComponentProps<typeof NextThemesProvider> (full pass-through)
- **Exports:** ThemeProvider

#### RootLayout
- **Type:** React (server)
- **Purpose:** App Router root layout. Loads Geist and Geist_Mono with CSS variable names --font-sans and --font-mono so they feed Tailwind v4's default font-sans/font-mono utilities without needing a @theme font mapping. Applies font variable classes + h-full + antialiased on html, sets suppressHydrationWarning for next-themes, imports @/styles/globals.css (task-15 stylesheet with active @theme inline block), exports base metadata (title default/template pair, description, openGraph image), and wraps body children in ThemeProvider.
- **Location:** src/app/layout.tsx
- **Props:** { children: React.ReactNode }
- **Exports:** metadata, default (RootLayout)

### Functions

#### Font configuration (Geist, Geist_Mono)
- **Purpose:** next/font/google invocations at module scope in src/app/layout.tsx. Variable names changed from --font-geist-sans/--font-geist-mono to --font-sans/--font-mono so they directly feed Tailwind v4's default --font-sans/--font-mono theme tokens (no @theme font remap needed). adjustFontFallback is left enabled (default).
- **Location:** src/app/layout.tsx:5-13
- **Signature:** const geistSans = Geist({ variable: '--font-sans', subsets: ['latin'] }); const geistMono = Geist_Mono({ variable: '--font-mono', subsets: ['latin'] })
- **Exported:** No

### Integrations

#### Integration
- **Description:** Root layout now imports @/styles/globals.css instead of the CNA scaffold at ./globals.css, and src/app/globals.css has been deleted. This is the moment task 15's @theme inline block becomes runtime-active for the whole site — Tailwind utilities like bg-primary/text-primary-foreground now resolve via var(--primary)/var(--primary-foreground) which are defined on :root (light) and .dark (dark) in src/styles/tokens.css. Verified empirically via production build CSS inspection and live Playwright computed-style assertion.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

#### Integration
- **Description:** Layout wraps body children in ThemeProvider (attribute=class, defaultTheme=system, enableSystem, disableTransitionOnChange). next-themes toggles the .dark class on <html>, which triggers the @custom-variant dark (&:is(.dark *)) declaration in globals.css and the .dark token block in tokens.css. html carries suppressHydrationWarning so next-themes' pre-hydration class injection does not produce a hydration mismatch warning.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

