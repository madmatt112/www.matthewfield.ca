# Implementation Log: Task 8.3

**Summary:** Implemented `checkProjectHeadings(meta)` heading-hygiene helper as a sibling module at `src/lib/build/check-project-headings.ts` (v4 prefers sibling-module path per Task 14.4 prerequisite). The helper re-parses raw MDX via `unified().use(remarkParse).use(remarkGfm).use(remarkMdx)`, walks with `unist-util-visit`, and enforces Reqs 6.9.a (no h1 — both mdast depth-1 and `<h1>/<H1>` mdxJsx tags), 6.9.b (depth>=4 rejected unless `PROJECTS_ALLOW_H4=1`), and 6.9.c (first heading must be h2; no deeper level-skips). The sequence rule fires regardless of `PROJECTS_ALLOW_H4`. Failures throw a named `ProjectHeadingHygieneError` carrying file path, depth, and rule. Wired into `velite.config.ts` as transform step 5 of the `projects` collection (after step 4 links uniqueness, before return). AST walk is the SOLE inspection mechanism — no regex pass over `meta.content`.

**Timestamp:** 2026-05-26T19:23:54.307Z
**Log ID:** 4c5589ff-37cd-40af-904a-8428b2f62a16

---

## Statistics

- **Lines Added:** +145
- **Lines Removed:** -2
- **Files Changed:** 2
- **Net Change:** 143

## Files Modified
- velite.config.ts

## Files Created
- src/lib/build/check-project-headings.ts

---

## Artifacts

### Functions

#### checkProjectHeadings
- **Purpose:** AST-only heading-hygiene check for projects MDX: rejects h1 (mdast + mdxJsx), depth>=4 unless PROJECTS_ALLOW_H4=1, enforces h2-first heading + no-deeper-level-skip on the linear sequence of mdast heading nodes. Throws ProjectHeadingHygieneError on failure. Importable for Task 14.4 unit tests.
- **Location:** src/lib/build/check-project-headings.ts:46
- **Signature:** export function checkProjectHeadings(meta: { content: string; path: string }): void
- **Exported:** No

### Classes

#### ProjectHeadingHygieneError
- **Purpose:** Named error class with file, depth, and rule fields for Reqs 6.9.a/6.9.b/6.9.c violations. Rule values: 'no-h1-mdast' | 'no-h1-jsx' | 'no-h4-plus' | 'first-heading-must-be-h2' | 'no-level-skip' | 'mdx-parse-failure'.
- **Location:** src/lib/build/check-project-headings.ts:18
- **Methods:** constructor({ file, rule, depth?, message })
- **Exported:** No

### Integrations

#### Integration
- **Description:** Sibling helper imported at top of velite.config.ts and invoked as step 5 inside the projects collection transform, after step 4 (links uniqueness) and before the return value. Passes `meta.content ?? ""` (Velite types content as string|undefined) and the relative file path for diagnostics. Step 6 (draft-warning emit) remains TODO in Task 8.4.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

