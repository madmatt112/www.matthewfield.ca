---
id: e8790260-7897-4f7f-80d4-4004e4058a92
taskId: "28.4"
specName: project-showcase
version: 1
verdict: fail
timestamp: 2026-05-28T03:47:35.672Z
criticalCount: 1
warningCount: 1
infoCount: 2
---

# Task Review: Task 28.4 (v1)

**Spec:** project-showcase
**Reviewed:** 2026-05-28T03:47:35.672Z
**Verdict:** fail

---

## Summary

Three artefacts are correct and on-spec (script logic, runs log content, ci.yml integration), but the implementation log omits BOTH the negative-case and positive-case CI workflow run URLs required by AC clause (ii) — and the positive case was substituted with a local-tmpdir scripted-fail rather than a remote workflow run. 28.4 cannot be marked [x] until both URLs are captured from real workflow runs.

## Findings

### CRITICAL: Missing both workflow run URLs required by AC clause (ii)
- **File:** .spec-workflow/specs/project-showcase/Implementation Logs/task-28-4_2026-05-28T0345_f9614612.md
- **Issue:** Task 28.4 line 806 (`tasks.md`): "Mark complete only when: (i) script + log + ci.yml modification all committed; (ii) both verification runs cited." The implementation log at task-28-4_2026-05-28T0345_f9614612.md contains neither URL — no negative-case CI run URL (launch state, 0 published, cadence step green) and no positive-case CI run URL (feature/cadence-positive-test branch, cadence step red). The positive case was substituted with a local-tmpdir scripted simulation because remote push was not pre-authorized. Blocker: user must (a) push the current changes to capture the negative-case run URL, AND (b) push the throwaway feature/cadence-positive-test branch to capture the positive-case run URL (then delete branch). Both URLs must be added to the implementation log before 28.4 can move from [-] to [x].
- **Task Requirement:** Task 28.4 AC (ii) — both verification runs cited

### WARNING [hygiene]: Implementation log omits local smoke-test record
- **File:** .spec-workflow/specs/project-showcase/Implementation Logs/task-28-4_2026-05-28T0345_f9614612.md
- **Issue:** The user-facing review brief expected the implementation log to record the local smoke (negative + positive simulated case). The log file (44 lines total) contains only the summary, statistics, files lists, and the function/integration artifacts — no `## Verification` / `## Smoke Test` section narrating either the negative local run (0 published → exit 0) or the positive simulated run (tmpdir scripted-fail proving exit 1 on delta≥3 ∧ N%3===0). Recommend appending a Verification section to the log before merge.

### INFO: Script artefact, runs log, and ci.yml integration verify clean
- **Issue:** `scripts/check-lighthouse-cadence.mjs` correctly applies `!/^fixture-/.test(p.slug)` and `!p.draft` (lines 53–55), threshold `delta >= 3 AND current % 3 === 0` (line 89), and parses `- Published projects at run time: N` from runs log (line 69). `docs/projects-showcase-lighthouse-runs.md` has Run 1 — launch (2026-05-27), initial count 0, all 8 launch scores from Task 28.3 transcribed verbatim (/projects 100/98/100/100; /projects/fixture-placeholder 100/96/100/100), and a JSON-IDs table citing lighthouseVersion 12.1.0 + fetchTime. `.github/workflows/ci.yml` line 8 adds `workflow_dispatch:` to the top-level on-block, and lines 131–133 add `name: "Check Lighthouse cadence"` (verbatim) immediately after `"Verify production build (Build 2)"` (lines 125–129), running `node scripts/check-lighthouse-cadence.mjs`. No separate `lighthouse-cadence.yml` workflow file present — v4 trigger collapse honored.

### INFO [hygiene]: Cadence-log latest-entry parser ordering — note for future runs
- **File:** scripts/check-lighthouse-cadence.mjs:69
- **Issue:** `check-lighthouse-cadence.mjs` line 69 uses the FIRST match of `- Published projects at run time: N` via `/m` (not `/gm` with last-match). The runs log convention pinned in `docs/projects-showcase-lighthouse-runs.md` (line 14 = `## Run 1 — launch`) is therefore latest-entry-on-top. Append-on-top is fine for now, but a future contributor appending at the bottom would silently parse Run 1's count instead of the latest. Worth a brief note in the runs log to enforce the convention (the script comment on lines 60–65 already discusses this but resolves to first-match — keep them aligned).

