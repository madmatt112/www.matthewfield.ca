# Implementation Log: Task 20

**Summary:** Created src/app/sitemap.ts exporting a default sitemap() that returns a MetadataRoute.Sitemap for the 13 known routes (/, /profile, /projects, /contributions, /blog, /resources, /playground, /about, /contact, /colophon, /now, /sitemap, /slashes). Each entry's url is built with `new URL(route, siteConfig.url).toString()` so the base URL comes from siteConfig.url, and lastModified is set to a single `new Date()` captured once per invocation. Build emits /sitemap.xml as a static route (Route listing shows `○ /sitemap.xml`). OG image path is already referenced in metadata via siteConfig.ogImage (wired into openGraph.images in src/app/layout.tsx from task 18); the binary at public/images/og-default.png is the manual prerequisite called out in the task spec.

**Timestamp:** 2026-04-21T21:10:00.092Z
**Log ID:** 7d3988ba-0a7c-475f-b741-8ab3178d5c68

---

## Statistics

- **Lines Added:** +26
- **Lines Removed:** -0
- **Files Changed:** 1
- **Net Change:** 26

## Files Modified
_No files modified_

## Files Created
- src/app/sitemap.ts

---

## Artifacts

### Functions

#### sitemap
- **Purpose:** Default export consumed by Next.js App Router to emit /sitemap.xml. Maps a static `routes` array of 13 paths to `{ url, lastModified }` entries, with url resolved via `new URL(route, siteConfig.url).toString()` so the base URL is driven by src/config/site.ts. lastModified is captured once per invocation. Downstream specs extend the `routes` array (or refactor to pull from collections) to register dynamic routes.
- **Location:** src/app/sitemap.ts:20
- **Signature:** export default function sitemap(): MetadataRoute.Sitemap
- **Exported:** No

