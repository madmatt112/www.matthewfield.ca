# Implementation Log: Task 27

**Summary:** Addressed review v1 warnings: added explicit toHaveCount(6) assertion on links within the 'Sections' region to enforce R6 AC2 cardinality (prevents silent pass if siteConfig.heroCards drifts), and replaced new RegExp(card.title) with plain-string accessible-name matching for parity with navigation.test.ts and to avoid regex-metacharacter fragility on future title changes. INFO finding about relative import vs @/ alias left unchanged — matches sibling e2e convention and is not a regression.

**Timestamp:** 2026-04-23T16:11:47.952Z
**Log ID:** 08d5437a-e54c-4a7d-a073-e82ff6ef2ead

---

## Statistics

- **Lines Added:** +4
- **Lines Removed:** -3
- **Files Changed:** 1
- **Net Change:** 1

## Files Modified
- /home/mcf/repo/matthew-field.ca/e2e/tests/landing.test.ts

## Files Created
_No files created_

---

## Artifacts

### Integrations

#### Integration
- **Description:** Revised landing-page Playwright suite (e2e/tests/landing.test.ts). The render test now combines (1) page.getByRole('region', { name: 'Sections' }).getByRole('link').toHaveCount(6) — explicit cardinality gate tying R6 AC2 to a hard number — with (2) per-card visibility assertions using plain-string accessible-name matching getByRole('link', { name: card.title }) (Playwright substring-matches; link accessible name already includes the CardDescription so the title substring is sufficient). Click tests likewise switched from new RegExp(card.title) to plain-string names, matching the convention in e2e/tests/navigation.test.ts. Tests still iterate over siteConfig.heroCards from src/config/site.ts and scope to the aria-labelledby="sections-heading" region (sr-only h2 'Sections') to disambiguate hero-card links from duplicate nav links.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** Test → page.goto('/') → Next.js renders src/app/(site)/page.tsx (server component) which maps siteConfig.heroCards → <HeroCard> → <Link href> wrapping shadcn Card → Playwright scopes to 'Sections' region, asserts link count = 6, asserts each title is visible, and asserts click navigation.

