# Implementation Log: Task 29

**Summary:** Added a Vitest spec that verifies the Velite `safeBodyHtml` transform strips `data-copy-source="…"` from `bodyHtml` (RSS-bound) while leaving the attribute intact in `body` (compiled MDX), and that the rendered `/feed.xml` output contains no `data-copy-source` even when the fixture is included. The spec uses a new draft fixture `fixture-unicode-code.mdx` whose code block contains a non-ASCII sparkle (U+2728) so any Unicode-naive edit to the strip regex would surface as a test break. The fixture is loaded through `getPublishedPosts()` with `BLOG_INCLUDE_DRAFTS=1` (set/restored in beforeAll/afterAll), keeping the existing `no-restricted-imports` chokepoint guard intact. Falsifier check confirmed: commenting out the `\sdata-copy-source="[^"]*"` replace in `velite.config.ts` causes assertions (a) and (c) to fail; (b) is structurally orthogonal and still passes. The strip line was restored afterwards. `pnpm vitest run src/lib/velite-safe-body-html.test.ts` → 5/5 pass. `pnpm typecheck` → clean. `pnpm lint` → no new errors from this change (pre-existing errors in unrelated files unchanged).

**Timestamp:** 2026-05-22T17:09:23.918Z
**Log ID:** bd70eeaa-bc38-47e4-8d5c-9aaf49463368

---

## Statistics

- **Lines Added:** +87
- **Lines Removed:** -1
- **Files Changed:** 3
- **Net Change:** 86

## Files Modified
- /home/mcf/repo/matthew-field.ca/.spec-workflow/specs/blog-enhanced/tasks.md

## Files Created
- /home/mcf/repo/matthew-field.ca/src/lib/velite-safe-body-html.test.ts
- /home/mcf/repo/matthew-field.ca/content/posts/fixture-unicode-code.mdx

---

## Artifacts

### Functions

#### velite-safe-body-html.test.ts (vitest suite)
- **Purpose:** Regression coverage for Velite `safeBodyHtml` data-copy-source strip and RSS render absence; non-ASCII fixture guards against Unicode-naive regex edits.
- **Location:** src/lib/velite-safe-body-html.test.ts
- **Signature:** describe('Velite safeBodyHtml — data-copy-source RSS strip') with 5 assertions: fixture loads, (a) bodyHtml lacks data-copy-source, (b) body retains data-copy-source, (c.1) feed includes fixture, (c.2) feed XML lacks data-copy-source
- **Exported:** No

### Integrations

#### Integration
- **Description:** undefined
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

