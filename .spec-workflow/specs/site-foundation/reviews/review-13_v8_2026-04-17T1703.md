---
id: 21b80f67-d88a-4b98-a4c2-89a5d83f77f3
taskId: "13"
specName: site-foundation
version: 8
verdict: findings
timestamp: 2026-04-17T17:03:46.892Z
criticalCount: 0
warningCount: 0
infoCount: 3
---

# Task Review: Task 13 (v8)

**Spec:** site-foundation
**Reviewed:** 2026-04-17T17:03:46.892Z
**Verdict:** findings

---

## Summary

v7's three warnings (AC3-via-Button, typography-bug-as-assertion, EXPECTED_INHERITED_FONT_FAMILY brittleness) and the dev/prod parity evidence gap are all addressed. Two new low-severity issues: the production reporter output recorded in spike-summary is stale by a one-line edit (dev reporter matches current file, production does not), and the negation list used to detect typography-fix regression overlaps with the intended playground stack so a future fix will trip three not.toContain assertions, not just the documented one.

## Findings

### INFO [hygiene]: Production reporter excerpt in spike-summary is stale relative to current test file
- **File:** e2e/spike-summary.txt:69
- **Issue:** spike-summary.txt:69-90 (the production-build reporter output) cites test source lines 174, 194, 241, 264, 301, 350, 387 — but in the current test file the corresponding tests are at 174, 195, 242, 265, 302, 351, 388. Every test except the first (plain div, line 174) is off by one. The dev-build reporter excerpt at lines 102-121 matches the current file exactly. The simplest explanation is that production tests were run, results recorded, then a single line was added to the test file (somewhere between the plain-div test ending at 193 and the shadcn-Button-utility test that now begins at 195), and dev-mode tests were re-run after the edit but production was not. The implementation summary claims spike-summary is the empirical artifact written from actual test execution, and tasks.md is listed as the only modified file — but the line-number drift indicates the test file was edited after the production run was captured. The assertions are unchanged enough that the prod run would still pass, but the verbatim reporter text in the artifact does not correspond to the current code. Re-run `pnpm build && pnpm test:e2e` once more and replace the production reporter block so the artifact is internally consistent (or add a one-line note explaining the line-number offset).
- **Task Requirement:** spike-summary.txt must reflect actual test execution results
- **Classification:** novel

### INFO: Negation list in Test 2 will fail for the wrong reason after the typography fix
- **File:** e2e/tests/playground-isolation.test.ts:93
- **Issue:** HOST_FONT_FAMILY_FRAGMENTS = ['Geist', 'Arial', 'Helvetica'] is asserted via not.toContain on the shadcn Button's font-family in Test 2 (e2e/tests/playground-isolation.test.ts:236-238). The intended playground stack in src/styles/playground.css:24-33 is `ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif` — note that Arial AND "Helvetica Neue" both appear in the intended stack. When the SPIKE FINDING is fixed and the @layer playground re-declarations actually apply, the computed font-family will include 'Arial' and 'Helvetica' (substring of 'Helvetica Neue'), tripping not.toContain('Arial') AND not.toContain('Helvetica') in addition to the intended ui-sans-serif flip the comment at lines 215-218 anticipates. The future maintainer will see three failing assertions and infer three regressions when the truth is one fix landing. Either narrow the negation list to fragments that cannot legitimately appear in the playground stack (just 'Geist'), or extend the comment to spell out which assertions to remove vs. invert when the playground.css typography fix lands.
- **Task Requirement:** R11 AC3 / R11 AC2
- **Classification:** novel

### INFO: Dark-mode harness still pre-empts a ThemeProvider race that doesn't exist yet
- **File:** e2e/tests/playground-isolation.test.ts:153
- **Issue:** applyDarkMode (e2e/tests/playground-isolation.test.ts:153-171) writes localStorage 'theme'='dark' AND attaches a DOMContentLoaded redundancy in addition to the manual classList.add. There is still no ThemeProvider in this build (task 16 unshipped), so the localStorage write and DOMContentLoaded handler are dead code today. v7 already flagged the brittleness; this iteration adds a follow-up procedure in spike-summary.txt:365-385 (re-audit Tests 4/5 immediately after task 16 ships) which is appropriate visibility for the deferral, but the dead-code cost in the test file remains and the assertion-survives-next-themes claim is unverified until task 16 lands. Acceptable for now given the explicit follow-up; flagging only so the next reviewer doesn't re-discover.
- **Task Requirement:** R11 AC5
- **Classification:** recurring

