# Implementation Log: Task 26

**Summary:** Implemented scripts/verify-getPublishedPosts-callers.mjs — a static-source audit that uses `git grep` to find `getPublishedPosts()` callers and enforces an ALLOWED_CALLERS allow-list (src/lib/blog.ts, src/lib/blog.test.ts, src/app/(site)/blog/[slug]/page.tsx). src/lib/blog-taxonomy.ts is intentionally NOT in the allow-list — taxonomy code MUST go through getVisiblePublishedPosts(). The verifier strips inline `//` comments via line.split("//")[0] then re-asserts the regex match, skips JSDoc/comment-only lines (^\s*(\*|//|/\*)) and backtick-quoted documentation strings. Non-zero exit on violations with a remediation hint; "OK" + exit 0 on clean. Wrapped execSync in a try/catch so git grep status=1 (no matches) is treated as clean. Companion test runs the verifier via execSync against the current codebase and asserts (a) exit 0, (b) stdout matches /OK/, (c) src/lib/blog-taxonomy.ts is absent from output. Adapted the design's "Vitest test" to node:test because vitest's test.include glob only covers src/**, while scripts/*.test.mjs use node --test (matching verify-chosen-path.test.mjs and verify-ci-topology.test.mjs convention).

**Timestamp:** 2026-05-22T16:24:15.414Z
**Log ID:** 3dee9342-99a9-46b8-a087-5c005066a264

---

## Statistics

- **Lines Added:** +115
- **Lines Removed:** -1
- **Files Changed:** 3
- **Net Change:** 114

## Files Modified
- /home/mcf/repo/matthew-field.ca/.spec-workflow/specs/blog-enhanced/tasks.md

## Files Created
- /home/mcf/repo/matthew-field.ca/scripts/verify-getPublishedPosts-callers.mjs
- /home/mcf/repo/matthew-field.ca/scripts/verify-getPublishedPosts-callers.test.mjs

---

## Artifacts

### Functions

#### verify-getPublishedPosts-callers (CLI script)
- **Purpose:** Static-source audit: enforces allow-list of files permitted to call getPublishedPosts() directly. All list-context callers must use getVisiblePublishedPosts() to ensure hiddenFromLists:true posts are filtered.
- **Location:** scripts/verify-getPublishedPosts-callers.mjs
- **Signature:** node scripts/verify-getPublishedPosts-callers.mjs — exits 0 with stdout 'OK' on clean; exits 1 with diagnostic on stderr listing each violating file (path: snippet) and remediation hint.
- **Exported:** No

### Integrations

#### Integration
- **Description:** Per design §'scripts/verify-getPublishedPosts-callers.mjs', this script SHALL be added to .github/workflows/ci.yml as a step named 'Verify getPublishedPosts callers' between 'Typecheck' and 'Unit tests' (wired in a later task). scripts/verify-ci-topology.mjs ordered-step list extension SHALL include this step name.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

