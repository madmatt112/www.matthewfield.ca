---
id: 40fcba60-9abe-4788-b518-a6b51adca097
taskId: "28.2"
specName: project-showcase
version: 1
verdict: findings
timestamp: 2026-05-28T03:33:05.895Z
criticalCount: 0
warningCount: 1
infoCount: 1
---

# Task Review: Task 28.2 (v1)

**Spec:** project-showcase
**Reviewed:** 2026-05-28T03:33:05.895Z
**Verdict:** findings

---

## Summary

Artefact, dimensions, viewport, Build-state pin and visual escape evidence all check out. One warning: the implementation log does not directly confirm the reused (EADDRINUSE) server was launched with PROJECTS_INCLUDE_DRAFTS=1; HTTP 200 is only indirect Build-1 evidence.

## Findings

### WARNING: Reused dev server's Build-1 state not directly confirmed
- **File:** .spec-workflow/specs/project-showcase/Implementation Logs/task-28-2_2026-05-28T0331_a1191f1e.md
- **Issue:** Implementation log states `pnpm start --port 3013` was used and HTTP 200 was verified for /projects/fixture-placeholder, which would 404 under Build 2. However, per the review brief, `pnpm start` hit EADDRINUSE and the script reused an already-running server; the log does not capture the reused server's process env (no `ps -o command`/env-snapshot citation) to prove PROJECTS_INCLUDE_DRAFTS=1 was set on that PID. HTTP 200 is consistent with Build 1 but indirect — a stale Build-2 server with a leftover Build-1 .next/ output could produce the same response. Add a direct env-pin citation (e.g., the build command stderr from 28.1's `PROJECTS_INCLUDE_DRAFTS=1 pnpm build` plus a `ps`/`/proc/<pid>/environ` snapshot of the listening server) to close the Build-state pin requirement unambiguously.
- **Task Requirement:** Restriction: Build 1 state ONLY — Build-state pinned in the implementation log

### INFO [hygiene]: Capture script removed without commit reference
- **File:** .spec-workflow/specs/project-showcase/Implementation Logs/task-28-2_2026-05-28T0331_a1191f1e.md
- **Issue:** The temporary Playwright capture script was deleted post-capture (per the task pin — artefact under docs/, not a permanent script). The log notes this but offers no commit SHA or transient-file path, so the exact capture command (viewport assertion, fullPage flag, navigation URL) cannot be re-derived from the repo. The artefact itself is what the task requires, so this is informational only — but a one-line inline snippet of the script's `page.screenshot({fullPage:true})` + `viewport: {width:1280,height:720}` call in the log would make the capture provenance fully self-contained.

