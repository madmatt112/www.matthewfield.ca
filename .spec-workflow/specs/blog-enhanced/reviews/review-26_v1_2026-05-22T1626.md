---
id: 5d7207e8-9fe8-476e-9586-b5338d981633
taskId: "26"
specName: blog-enhanced
version: 1
verdict: findings
timestamp: 2026-05-22T16:26:16.783Z
criticalCount: 0
warningCount: 0
infoCount: 3
---

# Task Review: Task 26 (v1)

**Spec:** blog-enhanced
**Reviewed:** 2026-05-22T16:26:16.783Z
**Verdict:** findings

---

## Summary

Verifier and companion test correctly implement Task 26: ALLOWED_CALLERS omits blog-taxonomy.ts, inline-comment strip uses line.split("//")[0] + regex re-assert, verifier exits 0 on the current tree, and a planted call in blog-taxonomy.ts is named in the diagnostic (confirmed via `git add -N`). The node:test adaptation is justified. Two non-blocking observations recorded.

## Findings

### INFO: node:test instead of vitest is a reasonable adaptation
- **File:** /home/mcf/repo/matthew-field.ca/scripts/verify-getPublishedPosts-callers.test.mjs
- **Issue:** Companion test uses node:test rather than vitest because vitest.config.ts test.include only covers src/** — scripts/*.test.mjs follow the existing project convention (verify-chosen-path.test.mjs, verify-ci-topology.test.mjs). The test runs cleanly via `node --test` and asserts (a) exit 0, (b) /OK/ in stdout, (c) blog-taxonomy.ts absent from output. No coverage gap.

### INFO: git grep only scans tracked/indexed content
- **File:** /home/mcf/repo/matthew-field.ca/scripts/verify-getPublishedPosts-callers.mjs:25
- **Issue:** The verifier uses `git grep`, which by default ignores untracked files. With src/lib/blog.ts, blog-taxonomy.ts, and the slug page currently untracked, the verifier vacuously prints OK against the local working tree. This is fine for CI (where the PR commits tracked files) and is implicit in the leverage choice (git grep), but means local pre-commit runs against untracked changes will under-report. Planted-call verification only fails after `git add -N` or staging. Not a defect — flagged for awareness.

### INFO [hygiene]: Hygiene console.* signals are intentional
- **Issue:** console.error at line 61 is the violation diagnostic; console.log at line 69 is the OK marker. Both are required CLI output. Not leftovers. Coverage gap from tsc: both .mjs files are excluded from tsc (expected for ESM JS scripts) — manual scan shows no implicit any / mismatched shapes; the code is straightforward Node built-ins.

