# Implementation Log: Task 18

**Summary:** Task 18 — Atomic prose-migration across the six long-form bodies. Migrated all six long-form bodies to the canonical `prose dark:prose-invert max-w-measure` class set in ONE change (no mid-migration state). (1) profile/page.tsx article: added `prose dark:prose-invert` to Task 17's `max-w-measure` body, dropping the now-redundant `text-base leading-relaxed text-foreground`; preserved `profile-print-root` and Task 17's kicker/headline/CTA. (2) about/now/colophon: converted each body `<div class="text-base leading-relaxed text-foreground">` to `prose dark:prose-invert max-w-measure` (themed scale owns body text). (3) blog/[slug] (line 143): replaced `max-w-[75ch]` with `max-w-measure` and DROPPED `prose-lg` so the themed scale owns sizing → `prose dark:prose-invert max-w-measure`. (4) projects/[slug]: removed `max-w-prose` from the OUTER wrapper (kept `mx-auto mt-8`) and put `max-w-measure` on the INNER `.prose` element so a single element carries the 75ch measure. No `max-w-none` anywhere; no surviving `max-w-[75ch]`/`max-w-prose`/`prose-lg`/`leading-relaxed` on any prose body (grep-verified). MDX content untouched. Updated e2e/tests/projects-detail-layout.test.ts: `PROSE_MAX_WIDTH` constant changed from 700 to 825 — the new 75ch measure was MEASURED at 825px by the actual Playwright Chromium runner against a built fixture-placeholder page at 1280×720 (a temporary console.log + an intentional assertion failure surfaced the real px; both were removed afterward). NOTE: the actual measured width (825px) is WIDER than the old 700px ceiling and wider than the task's predicted ~600px — the prose body face's `ch` unit resolves 75ch to ~825px in the test Chromium, so the original 700 ceiling would have failed; 825 (+20 tolerance = 845) is the correct green value. Comment updated to "75ch ≈ 825px (measured)". Checks: pnpm typecheck pass; pnpm build pass; targeted projects-detail-layout Playwright test GREEN (run 3× for stability); feed.xml parity Vitest pass (uses generic article/prose-scope selectors, unaffected).

**Timestamp:** 2026-06-09T03:16:25.584Z
**Log ID:** bead0f46-7ece-41b7-aa2b-1b141591de81

---

## Statistics

- **Lines Added:** +7
- **Lines Removed:** -8
- **Files Changed:** 7
- **Net Change:** -1

## Files Modified
- src/app/(site)/profile/page.tsx
- src/app/(site)/about/page.tsx
- src/app/(site)/now/page.tsx
- src/app/(site)/colophon/page.tsx
- src/app/(site)/blog/[slug]/page.tsx
- src/app/(site)/projects/[slug]/page.tsx
- e2e/tests/projects-detail-layout.test.ts

## Files Created
_No files created_

---

## Artifacts

### Integrations

#### Integration
- **Description:** Atomic prose-migration: all six long-form bodies (profile, about, now, colophon, blog/[slug], projects/[slug]) now use the canonical class set `prose dark:prose-invert max-w-measure` (verified in Task 5). The bare `max-w-measure` (utilities layer, 75ch) wins over the typography plugin's components-layer `.prose{max-width:65ch}` via v4 layer order. NEVER add `max-w-none` (it emits after max-w-measure and removes the cap). For projects/[slug] the 75ch measure is carried by the INNER `.prose` element; the outer wrapper keeps only `mx-auto mt-8`. The rendered 75ch measure on the SANS body face is ~795px in standard Chromium and ~825px in Playwright's bundled Chromium (font-metric difference) — the e2e ceiling constant PROSE_MAX_WIDTH=825 reflects the value MEASURED by the actual test runner, not the standalone browser.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

