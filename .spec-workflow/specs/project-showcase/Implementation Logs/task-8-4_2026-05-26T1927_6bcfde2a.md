# Implementation Log: Task 8.4

**Summary:** Added step 6 (draft-warning emit) to the projects transform in velite.config.ts — guarded by `data.draft === true && process.env.PROJECTS_INCLUDE_DRAFTS === "1"`, using `console.error` with the verbatim load-bearing single-process-pin comment block above it (rollback signal for a future worker-threads velite upgrade). Registered the `projects` collection LAST in `defineConfig({ collections: { pages, profile, posts, projects } })`. Created empty `content/projects/` directory so velite has the glob target.

**Timestamp:** 2026-05-26T19:27:18.448Z
**Log ID:** 6bcfde2a-28df-466c-a7b6-3f8764fa0b73

---

## Statistics

- **Lines Added:** +12
- **Lines Removed:** -2
- **Files Changed:** 3
- **Net Change:** 10

## Files Modified
- velite.config.ts
- .spec-workflow/specs/project-showcase/tasks.md

## Files Created
- content/projects/

---

## Artifacts

### Functions

#### projects collection transform (step 6 — draft-warning emit)
- **Purpose:** Step 6 of the projects transform pipeline — emits one console.error line per draft project per build when PROJECTS_INCLUDE_DRAFTS=1 is set, using the post-strip `slug` local. Includes the load-bearing single-process-pin comment block above the conditional (rollback signal for a future velite worker-thread upgrade that would break the one-emit-per-draft invariant; the Task 9 upgrade-gate test and Task 28.1 integration assertion detect that regression by warning-count > draft-fixture-count).
- **Location:** velite.config.ts:402-412
- **Signature:** transform((data, { meta }) => { ...; if (data.draft === true && process.env.PROJECTS_INCLUDE_DRAFTS === "1") console.error(`[velite/projects] PROJECTS_INCLUDE_DRAFTS=1 — including draft project: ${slug}`); return { ...data, slug }; })
- **Exported:** No

### Integrations

#### Integration
- **Description:** Added `projects` (LAST, after pages/profile/posts) to `defineConfig({ collections: { pages, profile, posts, projects } })`. Velite now emits `.velite/projects.json` on every build; downstream consumers (Task 10 `src/lib/projects.ts`, Task 9 output-shape test) can now import via `#site/content`. Verified: `pnpm typecheck` clean; `pnpm velite build` against empty `content/projects/` succeeds in ~895ms and emits `.velite/projects.json` containing `[]` (2 bytes).
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

