---
id: 9a6eeb34-ed40-4e22-85a5-36539791c099
taskId: "21"
specName: professional-profile
version: 1
verdict: findings
timestamp: 2026-05-15T17:16:13.068Z
criticalCount: 0
warningCount: 0
infoCount: 4
---

# Task Review: Task 21 (v1)

**Spec:** professional-profile
**Reviewed:** 2026-05-15T17:16:13.068Z
**Verdict:** findings

---

## Summary

Wrapper script implements all seven design steps faithfully; playwright config audit conclusion (no webServer.env set, so env inheritance works) is verified correct; structure.md edit is explicitly in scope per design §596. Two info-level observations only.

## Findings

### INFO: Playwright config audit conclusion verified
- **File:** e2e/playwright.config.ts:22
- **Issue:** Confirmed: e2e/playwright.config.ts:22-28 sets command, url, reuseExistingServer, stdout, stderr on webServer but does NOT set webServer.env at all. The PORT is supplied via the inline `--port ${PORT}` argument in the command string. Per the design Pin paragraph, the violation pattern (a literal `env: { PORT: '3013' }` that replaces inheritance) is absent — env inheritance from the wrapper's process.env into `pnpm start` works by default. No fix to playwright.config.ts is required. The audit ran and is documented in the implementation log.
- **Task Requirement:** Audit/fix playwright.config.ts per Pin paragraph

### INFO: structure.md edit scope is task-prescribed, not creep
- **File:** .spec-workflow/steering/structure.md:93
- **Issue:** structure.md changes (tree entry for scripts/ + run-e2e.mjs at lines 93-94, and Deployment & scripts paragraph at line 115 describing the wrapper) are explicitly mandated by design §596 (`structure.md updates`) which lists this exact addition as part of the task. The edit removes the `(future — lands in task 21)` annotation as the directory now exists. In-scope and necessary.
- **Task Requirement:** structure.md tree includes scripts/

### INFO [hygiene]: scripts/run-e2e.mjs uses process.execPath instead of literal 'node'
- **File:** scripts/run-e2e.mjs:31
- **Issue:** Design step 2 says `node e2e/fixtures/mock-resend.mjs`; implementation uses process.execPath (line 31). This is strictly better — guarantees the same Node binary the wrapper itself is running under, avoiding PATH ambiguity. No action needed; noting only because it diverges from the literal design text.

### INFO [hygiene]: Redundant --port arg to playwright webServer command (deferred per implementer)
- **File:** e2e/playwright.config.ts:23
- **Issue:** playwright.config.ts:23 spawns `pnpm start --port ${PORT}`; package.json `start` script already includes `--port 3013`. The duplicate is harmless (next start accepts and the value matches) but flagged by the implementer. Out of scope for task 21; the wrapper itself is correct.

