# Implementation Log: Task 21

**Summary:** Implemented E2E wrapper script scripts/run-e2e.mjs per design §Wrapper script (7 steps): allocates an ephemeral port via net.createServer().listen(0); spawns node e2e/fixtures/mock-resend.mjs with MOCK_PORT=<port> and stdio:['ignore','pipe','inherit']; races a 5s timeout against a READY chunk on stdout and exits non-zero with a diagnostic if the mock fails to start; exports RESEND_BASE_URL=http://127.0.0.1:<port>, RESEND_API_KEY=test-key, RESEND_FROM=onboarding@resend.dev, RESEND_TO=test@example.com, NEXT_PUBLIC_SITE_URL=http://localhost:3013 into process.env; registers cleanup on SIGTERM, SIGINT, and 'exit' that kills the mock child; spawns `pnpm exec playwright test --config=e2e/playwright.config.ts` with stdio inherit and no explicit env option (so the prepared env is inherited intact, per the design's Pin paragraph); on Playwright exit kills the mock and exits with Playwright's exit code (or 1 on signal). Updated package.json `test:e2e` script to `node scripts/run-e2e.mjs`. Audited e2e/playwright.config.ts — its `webServer` clause does NOT set `webServer.env` at all (PORT is supplied via the command-line `--port ${PORT}` argument), so env-inheritance from the wrapper into `pnpm start` is correct by default; no fix needed (per design Pin paragraph, the violation pattern is setting `webServer.env` to a literal object, which is not present). Updated structure.md to remove the `(future — lands in task 21)` annotation on scripts/ and updated the prose note in the Deployment & scripts paragraph to describe the now-existing scripts/run-e2e.mjs. Smoke-ran the wrapper: confirmed mock-resend printed READY, env was set, Playwright was spawned with --config=e2e/playwright.config.ts, and webServer launched `pnpm start`. typecheck passes, eslint passes (clean exit via rtk proxy).

**Timestamp:** 2026-05-15T17:13:56.694Z
**Log ID:** d827cbea-c0ad-46bc-8435-fe6d088e5251

---

## Statistics

- **Lines Added:** +135
- **Lines Removed:** -4
- **Files Changed:** 4
- **Net Change:** 131

## Files Modified
- /home/mcf/repo/matthew-field.ca/package.json
- /home/mcf/repo/matthew-field.ca/.spec-workflow/steering/structure.md
- /home/mcf/repo/matthew-field.ca/.spec-workflow/specs/professional-profile/tasks.md

## Files Created
- /home/mcf/repo/matthew-field.ca/scripts/run-e2e.mjs

---

## Artifacts

### Functions

#### allocatePort
- **Purpose:** Allocate an ephemeral free TCP port by binding net.createServer to port 0 on 127.0.0.1, then closing — returns the port number for the mock-resend sidecar.
- **Location:** scripts/run-e2e.mjs
- **Signature:** function allocatePort(): Promise<number>
- **Exported:** No

#### startMock
- **Purpose:** Spawn node e2e/fixtures/mock-resend.mjs with MOCK_PORT env, race a 5s timeout against a READY chunk on the child's stdout. Resolves with the child process or rejects with a diagnostic.
- **Location:** scripts/run-e2e.mjs
- **Signature:** function startMock(port: number): Promise<ChildProcess>
- **Exported:** No

#### main
- **Purpose:** Top-level wrapper orchestration: allocate port → start mock → export Resend env → register cleanup → spawn Playwright → propagate exit code.
- **Location:** scripts/run-e2e.mjs
- **Signature:** async function main(): Promise<void>
- **Exported:** No

### Integrations

#### Integration
- **Description:** undefined
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

#### Integration
- **Description:** undefined
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

