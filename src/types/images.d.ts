// Static image imports, e.g. `import cat from "@/assets/not-found/cat.jpg"`.
// Next declares these module types in the generated next-env.d.ts, which is
// gitignored and absent when CI runs `tsc --noEmit` before any build. This
// tracked reference makes the declarations available everywhere tsc runs.
/// <reference types="next/image-types/global" />
