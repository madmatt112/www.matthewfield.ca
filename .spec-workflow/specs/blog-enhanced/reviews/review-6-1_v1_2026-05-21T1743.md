---
id: c2eb5481-a0b7-4751-becc-d726108b6ae7
taskId: "6.1"
specName: blog-enhanced
version: 1
verdict: fail
timestamp: 2026-05-21T17:43:24.614Z
criticalCount: 3
warningCount: 2
infoCount: 1
---

# Task Review: Task 6.1 (v1)

**Spec:** blog-enhanced
**Reviewed:** 2026-05-21T17:43:24.614Z
**Verdict:** fail

---

## Summary

Schema additions (hiddenFromLists, excludeFromSearch, categories.max(3)) are correctly present, but the task's explicit "no transform changes in this task" restriction is violated: ~100 lines of transform body owned by Tasks 6.2/6.3/6.4/6.5 have been folded into this checkbox, plus out-of-scope top-level changes (shared plugin config, markdown block, bodyHtml field, new imports).

## Findings

### CRITICAL: Scope violation: transform body implemented in a schema-only task
- **File:** velite.config.ts:103
- **Issue:** Task 6.1 is unambiguous: 'Schema additions only' and 'No transform changes in this task. The schema landing alone unblocks Task 22 ... 6.2–6.5 follow.' The Restrictions field repeats this. The diff adds a full .transform((data, { meta }) => { ... }) body at velite.config.ts:103-203 implementing kebab-slug enforcement (Task 6.x kebab regex), the MDAST rejection visitor (Task 6.2 territory), reading-time computation, and safeBodyHtml CDATA/MDX-comment stripping (Task 6.5). This collapses 6.2, the parsing-pipeline portions of 6.3, and 6.5 into 6.1, defeating the deliberate per-checkbox decomposition. The implementation log even claims 'No transform changes in this task — 6.2 ... 6.5 follow.' — this is directly contradicted by the code.
- **Task Requirement:** Restrictions: 'Do NOT touch the transform body in this task.'

### CRITICAL: Out-of-scope schema field: bodyHtml
- **File:** velite.config.ts:100
- **Issue:** The schema adds `bodyHtml: s.markdown()` at velite.config.ts:100 alongside the three sanctioned fields. The task lists exactly three additions (hiddenFromLists, excludeFromSearch, categories.max(3) cap) — bodyHtml is not in that list. bodyHtml belongs to the RSS/feed work (Task 6.5 area, per the safeBodyHtml comment in the transform).
- **Task Requirement:** 'Add only the three schema fields.'

### CRITICAL: Out-of-scope top-level config: shared plugin arrays + markdown block
- **File:** velite.config.ts:23
- **Issue:** The diff introduces `prettyCodeOptions`, `sharedRemarkPlugins`, `sharedRehypePlugins` (velite.config.ts:23-34), wires them into a new top-level `markdown:` block (velite.config.ts:232-235), and extends `mdx:` to use them (line 228-231). None of this is part of Task 6.1, which scopes the change to the posts collection schema only. The new imports `path`, `remarkGfm`, `remarkParse`, `remarkMdx`, `rehypePrettyCode`, `unified`, `visit`, `siteConfig`, `rehypeAbsolutizeUrls`, `countWordsFromMdast` (velite.config.ts:2-13) all flow from this out-of-scope work.

### INFO: Sanctioned schema additions are correct
- **File:** velite.config.ts:91
- **Issue:** The three in-scope schema lines are correctly authored: `categories: s.array(s.string()).max(3).default([])` (line 91), `hiddenFromLists: s.boolean().optional()` (line 96), `excludeFromSearch: s.boolean().optional()` (line 97). The `.max(3)` cap composes correctly on top of `.array(s.string())` per the restriction. `pnpm exec velite build` accepts the existing blog-core fixtures (verified locally: 'build finished in 820.57ms'). The first success criterion is met for the schema portion.

### WARNING: Downstream tasks 6.2–6.5 will have nothing to implement
- **Issue:** Because the transform body, h4 rejection-adjacent visitor scaffolding (heading clause still missing), fixture-slug audit hook point, and safeBodyHtml are all landed here, the subsequent checkboxes become either no-ops or duplicative edits. Notably the h4 clause from Task 6.2 (Req 7.10) is NOT yet present in the visitor at lines 134-171, so 6.2 still has real work — but 6.3's fixture-slug import + 6.5's safeBodyHtml are effectively done. The decomposition the spec deliberately enforced is broken, and the review/log trail will no longer match the checkbox semantics.

### WARNING: Implementation log misrepresents the change
- **Issue:** implementationSummary states 'No transform changes in this task — 6.2 (h4 rejection), 6.3 (fixture-slug audit), 6.4/6.4.1 (series-order collision), and 6.5 (safeBodyHtml RSS strip) follow.' The diff shows a 100-line .transform body including the safeBodyHtml RSS strip explicitly named in the log as a future task. Either the log needs correction or the implementation needs to revert to schema-only.

