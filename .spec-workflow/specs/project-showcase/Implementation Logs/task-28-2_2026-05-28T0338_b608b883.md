# Implementation Log: Task 28.2

**Summary:** ### Build-1 pin retroactive evidence (warning action)

Re-captured the projects-showcase fixture render screenshot under a verified Build-1 (production `next start`) server with `PROJECTS_INCLUDE_DRAFTS=1`, after cleaning `.velite`/`.next`, running `velite build` (exit 0) and `pnpm build` (exit 0). Server confirmed listening on :3013 via `ss -ltnp` (pid=88012, next-server v16.2.2).

Three evidence pieces captured verbatim:

ps -p "$PID" -o pid=,cmd=
```
88012 next-server (v16.2.2)
```

cat /proc/$PID/environ | tr '\0' '\n' | grep -E '^(PROJECTS_INCLUDE_DRAFTS|NODE_ENV)='
```
PROJECTS_INCLUDE_DRAFTS=1
```
(NODE_ENV not present in /proc/<pid>/environ — Next.js sets process.env.NODE_ENV='production' internally for `next start` rather than inheriting it from the parent environment; presence of `next-server` binary in `ps` confirms production server.)

curl -s -o /dev/null -w "%{http_code}" http://localhost:3013/projects/fixture-placeholder
```
200
```

PNG verification:
```
docs/projects-showcase-fixture-render.png: PNG image data, 1280 x 2611, 8-bit/color RGB, non-interlaced
```

Screenshot captured via Playwright chromium, viewport 1280x720, fullPage:true.

**Timestamp:** 2026-05-28T03:38:17.264Z
**Log ID:** b608b883-3ca0-4a23-b1e0-b0c5e8d3b7af

---

## Statistics

- **Lines Added:** +0
- **Lines Removed:** -0
- **Files Changed:** 1
- **Net Change:** 0

## Files Modified
- docs/projects-showcase-fixture-render.png

## Files Created
_No files created_

---

## Artifacts

_No artifacts recorded_
