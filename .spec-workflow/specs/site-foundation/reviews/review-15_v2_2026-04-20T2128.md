---
id: 419cc4ee-4d41-489c-a2b0-1a86a5063302
taskId: "15"
specName: site-foundation
version: 2
verdict: findings
timestamp: 2026-04-20T21:28:09.138Z
criticalCount: 0
warningCount: 1
infoCount: 3
---

# Task Review: Task 15 (v2)

**Spec:** site-foundation
**Reviewed:** 2026-04-20T21:28:09.138Z
**Verdict:** findings

---

## Summary

V1 findings are acknowledged through deferrals and comment tightening, but task 15 still lists R9 AC3 as an in-scope requirement despite handing the visual verification to task 16, and the deferral for the dark-mode destructive /60 contrast records that math was skipped rather than computing it.

## Findings

### WARNING: Task 15 _Requirements still lists R9 AC3 despite carry-over to task 16
- **File:** .spec-workflow/specs/site-foundation/tasks.md:141
- **Issue:** tasks.md:141 keeps R9 AC3 in task 15's _Requirements line (`R9 AC1, R9 AC2, R9 AC3, R5 AC5`) while the implementation summary and task 16's _Requirements (`R9 AC3 (carry-over from task 15)`) explicitly defer the Button visual verification to task 16. This is inconsistent: either task 15 fulfils R9 AC3 and cannot be closed until Button is visually verified (which requires the runtime-inactive @theme block to be picked up — only possible after task 16 flips the stylesheet import), or R9 AC3 belongs to task 16 and must be removed from task 15's list (e.g., annotated as 'R9 AC3 (verified in task 16)'). As written, closing task 15 asserts a requirement that the implementation admits it cannot verify — the v1 finding about the @theme block being runtime-dead is structurally still present, just redirected.
- **Task Requirement:** R9 AC3
- **Classification:** compounding

### INFO: Deferral d-142fd010 records a gap instead of closing it
- **File:** .spec-workflow/deferrals/d-142fd010.md:19
- **Issue:** The v1 warning about dark-mode destructive /60 composited contrast asked for the as-rendered ratio to be computed. d-142fd010 documents that the math was 'straightforward but was skipped during task 15' and punts to 'the first accessibility audit pass'. The computation is ~5 lines of oklch→linear-sRGB→WCAG arithmetic; skipping it leaves R5 AC5 with no numerical bound on whether the rendered destructive Button even clears 3:1 (AA-large). A deferral that defers the work itself rather than a decision leaves the verification gap indistinguishable from 'we don't know if it passes'.
- **Task Requirement:** R5 AC5
- **Classification:** novel

### INFO: Inconsistent regeneration commands between tokens.css and globals.css comments
- **File:** src/styles/tokens.css:2
- **Issue:** tokens.css:1-4 still instructs future maintainers to regenerate via `npx shadcn@latest init --defaults`, while globals.css:19-20 now pins `npx shadcn@latest init --base radix --preset nova --yes --force` for the paired @theme block. The implementation summary claims these produce identical output (because neutral base is the shadcn default), but the provenance comments now disagree on which flags to use, which was part of what v1's INFO finding flagged. Either consolidate the two comments on one command or add a cross-reference note so the next regen pass doesn't split tokens and @theme on different upstream snapshots.
- **Task Requirement:** R9 AC1
- **Classification:** compounding

### INFO [hygiene]: Deferral d-7790c25b re-introduces new-york/neutral preset confusion
- **File:** .spec-workflow/deferrals/d-7790c25b.md:19
- **Issue:** v1 INFO [hygiene] flagged that 'new-york style' phrasing conflated shadcn's component-style preset with the 'neutral' base-color preset in globals.css. That was fixed in globals.css, but d-7790c25b's Context block now contains the same phrasing: it references 'the neutral preset's scaffold emitted by `npx shadcn@latest init --base radix --preset nova`' and 'shadcn's new-york destructive Button variant' in the same paragraph. Since deferrals are durable artifacts, this re-creates the provenance ambiguity v1 asked to be removed. Clarify which preset governs the Button rendering in one place (components.json's `style` field) and reference that from the deferral.
- **Task Requirement:** R5 AC5
- **Classification:** compounding

