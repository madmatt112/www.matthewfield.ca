# Implementation Log: Task 18.7

**Summary:** Created <CopyButton /> DOM-marker client hydrator (no props; useEffect queries [data-copy-button] and attaches click handlers; decodes data-copy-source via decodeUtf8B64; copies + announces; per-button state lives on the DOM via data-copy-state + aria-label swap; cleanup removes listeners and clears timers on unmount). Populated copy-button.css slice (positions .code-block-wrapper relative + [data-copy-button] absolute top-right at 2.75rem × 2.75rem = h-11 w-11 / 44 CSS px touch target; uses --foreground / --muted-foreground / --border tokens for theme parity; ::before content swaps Copy → Copied → Failed by state). Swapped the registry placeholder for a real fixture: a static .code-block-wrapper > pre + button[data-copy-button] (mirroring exactly what rehype-copy-button emits) PLUS a server-rendered #copy-status aria-live region PLUS the <CopyButton /> hydrator mounted below it. Added Playwright smoke that grants clipboard-read/write permissions, asserts 44x44 touch target, asserts the hydrator set data-copy-state="idle" on mount, clicks the button, asserts data-copy-state flips to "copied", asserts navigator.clipboard.readText() round-trips the decoded source, asserts the #copy-status region reads "Code copied", and asserts a computed-color delta between light + dark themes. Marker selector confirmed against src/lib/build/rehype-copy-button.ts: [data-copy-button] (also data-copy-source for the base64 payload). Typecheck clean.

**Timestamp:** 2026-05-22T02:10:25.830Z
**Log ID:** 71fd00f4-419e-40f6-a89a-a15ed6c0f30d

---

## Statistics

- **Lines Added:** +245
- **Lines Removed:** -6
- **Files Changed:** 5
- **Net Change:** 239

## Files Modified
- /home/mcf/repo/matthew-field.ca/src/styles/blog/copy-button.css
- /home/mcf/repo/matthew-field.ca/src/app/(site)/blog/__component-preview/registry/copy-button.tsx
- /home/mcf/repo/matthew-field.ca/.spec-workflow/specs/blog-enhanced/tasks.md

## Files Created
- /home/mcf/repo/matthew-field.ca/src/components/blog/copy-button.tsx
- /home/mcf/repo/matthew-field.ca/e2e/tests/component-preview/copy-button.test.ts

---

## Artifacts

### Components

#### CopyButton
- **Type:** React Client Component ("use client")
- **Purpose:** DOM-marker client hydrator. NOT instantiated per <pre> via JSX — mounted ONCE inside <article> on the post page (Task 20 will wire it). On mount, queries every [data-copy-button] emitted by rehype-copy-button.ts (Task 15) and attaches click handlers. Handler decodes the button's data-copy-source (base64 UTF-8) via decodeUtf8B64 from clipboard.ts (Task 17), calls copyToClipboard + announceCopyStatus, and flips per-button state via data-copy-state + aria-label (state lives on the DOM so one hydrator covers many buttons). State machine per button: idle -> copying -> copied (2s) -> idle; on error: copying -> failed (5s) -> idle. Cleanup: removes listeners + clears timers on unmount.
- **Location:** src/components/blog/copy-button.tsx
- **Props:** {} — no props (DOM-marker hydrator)
- **Exports:** CopyButton

### Integrations

#### Integration
- **Description:** CopyButton consumes copyToClipboard, announceCopyStatus, and decodeUtf8B64 from src/components/blog/clipboard.ts (Task 17). Button markers (data-copy-button + data-copy-source base64) are emitted at build time by src/lib/build/rehype-copy-button.ts (Task 15). announceCopyStatus updates the #copy-status aria-live region (server-rendered; Task 20 wires it on the post page; the preview-route registry entry includes one inline so the smoke can assert announcements).
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** rehype-copy-button.ts (build) -> <pre> wrapped in .code-block-wrapper with sibling <button data-copy-button data-copy-source='<b64>'> -> hydrator on mount -> click -> decodeUtf8B64 -> copyToClipboard -> announceCopyStatus -> #copy-status aria-live region

