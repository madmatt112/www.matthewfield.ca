# Implementation Log: Task 28.4

**Summary:** ## Verification — real CI workflow URLs

Follow-up to task-28-4_2026-05-28T0345_f9614612 and task-28-4_2026-05-28T0349_141b15fb (which captured the local-tmpdir simulation of the cadence guard). This entry cites the two real CI workflow run URLs the AC required.

### URL (a) — Negative case (push to main)

Run: https://github.com/madmatt112/www.matthewfield.ca/actions/runs/26593496468

Branch: `main` @ `df5aacb` (showcase implementation + lint/CI fixes).

Cadence-step verbatim output:
```
[lighthouse-cadence] OK — 0 published (fixtures excluded), last run at 0, next check at 3
```

Step conclusion: success (exit 0). Step ran via `if: always()` even though the overall run conclusion is `failure` (pre-existing Format check drift, unrelated to the cadence guard). Confirms green-path behavior end-to-end inside CI: dual-build Build 2 produces `.velite/projects.json`, cadence script reads it, current_count=0 (both fixtures filtered: one draft, one `^fixture-` prefix), exits 0.

### URL (b) — Positive case (workflow_dispatch on throwaway branch)

Run: https://github.com/madmatt112/www.matthewfield.ca/actions/runs/26593546255

Branch: `feature/cadence-positive-test` @ `7a220fb` — adds three published, non-fixture project mdx files (`content/projects/delete-me-cadence-{1,2,3}.mdx`) that push the non-fixture published count to 3 and trip the cadence threshold.

Cadence-step verbatim output:
```
[lighthouse-cadence] CADENCE TRIGGER — 3 published projects (fixtures excluded), last Lighthouse run at 0. Run Lighthouse against /projects and a representative /projects/<slug>, then append a new run entry to docs/projects-showcase-lighthouse-runs.md with "- Published projects at run time: 3".
```

Step conclusion: failure (exit 1, intended). Confirms red-path behavior end-to-end inside CI: delta=3 ≥ CADENCE_N=3 AND current_count % 3 === 0 → trigger fires, diagnostic names the threshold and the log file the operator must update.

### CI-topology fix bundled to unblock these runs

The cadence step lives after Build 2 in CI. Reaching it surfaced a pre-existing dual-build CI bug introduced by blog-core: the `Clean for Build 2` step wipes `.velite/` (which `pnpm install`'s postinstall had seeded for Build 1), but no subsequent step regenerates it before Build 2's `next build`. Build 2 was failing with `Module not found: '#site/content'` on every push to main since 2026-05-25. Added an explicit `Velite build (for Build 2)` step between `Clean for Build 2` and `Build 2 (production-mode)`. Also exempted three intentional-violation files from `no-restricted-imports` (`src/__fixtures__/chokepoint-canary.ts`, `src/lib/blog.test.ts`, `src/app/feed.xml/parity.test.ts`) and added `public/pagefind/**` to ESLint's globalIgnores. Both runs above are on the fixed CI topology.

### Conclusion

Both branches of the cadence guard exercised inside CI (not just locally), with verbatim step output and exit codes captured from named run URLs. Satisfies the AC's "two real CI workflow run URLs" requirement.

**Timestamp:** 2026-05-28T18:20:45.885Z
**Log ID:** 06a26776-ced0-4475-a0e8-a733cd5302e3

---

## Statistics

- **Lines Added:** +61
- **Lines Removed:** -1
- **Files Changed:** 5
- **Net Change:** 60

## Files Modified
- .github/workflows/ci.yml
- eslint.config.mjs

## Files Created
- content/projects/delete-me-cadence-1.mdx
- content/projects/delete-me-cadence-2.mdx
- content/projects/delete-me-cadence-3.mdx

---

## Artifacts

### Functions

#### check-lighthouse-cadence (CI verification)
- **Purpose:** Final verification of the cadence guard executing inside CI (not just locally). Captures two real workflow run URLs: (a) negative-case on main (run 26593496468, cadence step succeeded, exit 0, 0 non-fixture published) and (b) positive-case on feature/cadence-positive-test (run 26593546255, cadence step failed as intended, exit 1, CADENCE TRIGGER fires at 3 published). Both runs executed on CI topology post-fix — `Velite build (for Build 2)` step now regenerates .velite/ between the Clean step and Build 2, unblocking #site/content module resolution for the rest of the Build-2 chain.
- **Location:** /home/mcf/repo/matthew-field.ca/scripts/check-lighthouse-cadence.mjs
- **Signature:** node scripts/check-lighthouse-cadence.mjs (no args)
- **Exported:** No

### Integrations

#### Integration
- **Description:** undefined
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

