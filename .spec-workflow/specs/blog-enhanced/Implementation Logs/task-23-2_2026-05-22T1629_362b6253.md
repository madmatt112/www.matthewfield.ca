# Implementation Log: Task 23.2

**Summary:** Extended .github/workflows/ci.yml with the Vercel-block batch from Task 23.2: inserted four new sequential steps after Task 23.1's Pagefind block — "Check Vercel auto-deploy status", "Vercel build", "Verify Pagefind artifact in .vercel/output", "Vercel deploy (Build 2)" (id: vercel_deploy) — all gated on `vars.DEPLOY_VIA_CI == 'true'` (the artifact-verify step additionally requires `vars.PAGEFIND_ENABLED != 'false'`). Wired secrets/vars per scripts' env contracts: VERCEL_TOKEN, VERCEL_PROJECT_ID, VERCEL_ORG_ID, MIGRATION_DEADLINE on the check step; ORG/PROJECT IDs on Vercel build/deploy; GH_TOKEN, EVENT_NAME, PR_NUMBER, REF on the deploy step so downstream warn (Task 23.3) can consume DEPLOY_URL via `steps.vercel_deploy.outputs.deploy_url`. The deploy `run:` captures stdout from `vercel deploy --prebuilt` and writes `deploy_url=$DEPLOY_URL >> $GITHUB_OUTPUT` per design.md §"CI extensions". Separately inserted the "Verify getPublishedPosts callers" step (calls scripts/verify-getPublishedPosts-callers.mjs from Task 26) IMMEDIATELY AFTER "Typecheck" and BEFORE "Unit tests" per the verifier's enhanced-anchor pin. Step name literals verbatim per Task 25's pinned registry. `DEPLOY_VIA_CI` is referenced exclusively via `vars.*` — never via `env:` — per restriction. No draft PR opened: per the no-unauthorized-PR rule the draft-PR verification (which requires flipping DEPLOY_VIA_CI in repo Variables and respecting the Req 0.3 v4 safety pin against the production Vercel project) is deferred to the user; not marking [x] for the same reason. Verifier flag-off (`node scripts/verify-ci-topology.mjs`) PASSES; flag-on still reports "Warn deploying without Pagefind" missing — expected, that step lands in Task 23.3 along with the flag flip. `pnpm typecheck` and `pnpm lint` both green.

**Timestamp:** 2026-05-22T16:29:33.476Z
**Log ID:** 362b6253-3f95-4184-8b74-2e64844971cb

---

## Statistics

- **Lines Added:** +38
- **Lines Removed:** -0
- **Files Changed:** 2
- **Net Change:** 38

## Files Modified
- /home/mcf/repo/matthew-field.ca/.github/workflows/ci.yml
- /home/mcf/repo/matthew-field.ca/.spec-workflow/specs/blog-enhanced/tasks.md

## Files Created
_No files created_

---

## Artifacts

### Integrations

#### Integration
- **Description:** undefined
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

#### Integration
- **Description:** undefined
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

