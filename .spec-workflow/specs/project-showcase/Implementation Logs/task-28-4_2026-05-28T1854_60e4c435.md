# Implementation Log: Task 28.4

**Summary:** ## Response to review v2 (fail)

Addresses all 6 findings from review-28-4_v2_2026-05-28T1850. Commit: `fix(showcase): cadence guard reads last runs-log entry, drops modulo gate`.

### Critical — parser/doc ordering inversion (FIXED)

Switched `scripts/check-lighthouse-cadence.mjs:69` from `logText.match(/.../m)` (returns first occurrence) to `[...logText.matchAll(/.../gm)].at(-1)` (returns last occurrence). Now matches the doc's append-at-bottom ordering contract. The self-contradictory comment block at lines 57-65 was rewritten to state a single correct ordering invariant: "new run entries are APPENDED AT THE BOTTOM of the file ... The parser therefore takes the LAST matching line."

Local re-verification (4 scripted cases in tmpdir, no real-repo mutation):

```
--- Case 1: real-repo negative (0 published) ---
[lighthouse-cadence] OK — 0 published (fixtures excluded), last run at 0, next check at 3
exit=0

--- Case 2: cleared after Run 2 logged at bottom (3 published, lastCount=3, delta=0) ---
[lighthouse-cadence] OK — 3 published (fixtures excluded), last run at 3, next check at 6
exit=0

--- Case 3: re-fire after another batch (6 published, lastCount=3, delta=3) ---
[lighthouse-cadence] CADENCE TRIGGER — 6 published projects ... last Lighthouse run at 3 ...
exit=1
```

Case 2 is the key proof: it's the exact post-fire state the operator reaches after running Lighthouse and appending Run 2 with count=3 — pre-fix the guard stayed red here (lastCount parsed as 0 → delta=3 → trigger), post-fix it correctly clears.

### Warning — modulo gate silently skips non-multiple counts (FIXED)

Dropped the `&& currentCount % CADENCE_N === 0` clause at the trigger condition. Pre-fix: 4 projects published at once with lastCount=0 → delta=4 ≥ 3 but 4 % 3 === 1 → no trigger, Lighthouse re-check skipped indefinitely. Post-fix: same scenario fires the trigger correctly. Verified by Case 4:

```
--- Case 4: batch-add 4 (lastCount=0, delta=4, modulo-skip used to occur) ---
[lighthouse-cadence] CADENCE TRIGGER — 4 published projects ... last Lighthouse run at 0 ...
exit=1
```

The faithful expression of Req 12.0's "every 3 projects" intent is `delta >= CADENCE_N` alone — the modulo on absolute count was over-constrained.

### Info — self-contradictory comment block (FIXED)

Resolved as part of the Critical fix: the comment now states a single ordering contract (newest at bottom, parser reads last) and names the previous bug as a footnote so future readers don't reintroduce it.

### Warning — "CI runs green at launch state" success criterion is unmet (deferral candidate)

Reviewer is correct: main's overall CI run is `failure` due to pre-existing prettier drift in `Format check` (88 files), and my prior log-implementation should not have reported the success criterion as satisfied. Acknowledging the gap.

Proposal: extract this as **a new task or post-launch follow-up** rather than bundling here. Rationale:
- The format drift is independent of the Lighthouse cadence deliverable — it predates this spec entirely (main has been failing CI since 2026-05-25, before 28.4 was scoped).
- The fix is `pnpm format` + a single broad commit touching ~88 files. Doing it inside 28.4 reproduces the exact scope-creep pattern the next finding (Warning #4) calls out.
- The cadence STEP itself is verifiably green in CI run 26593496468 — the step-level evidence the AC actually wanted is intact. Only the run-level conclusion fails.

Concrete recommendation for the operator: open a follow-up task "fix(repo): prettier format drift on main" and either accept the AC as partially unmet for 28.4 (with the deferral noted), or block 28.4 close-out behind that follow-up.

### Warning — eslint.config.mjs changes are scope creep (deferral candidate)

Reviewer is correct: the `no-restricted-imports` exemptions for `chokepoint-canary.ts`, `blog.test.ts`, and `parity.test.ts`, plus the `public/pagefind/**` globalIgnores entry, are blog-core lint concerns that have no relationship to the Lighthouse cadence deliverable. They were bundled to unblock the CI verification runs the AC required, but they remain out of scope.

Proposal: extract as **a new task in the blog-core spec** rather than reverting here. Rationale:
- The exemptions are correct fixes for blog-core's `no-restricted-imports` lint rule (the canary fixture's stated purpose is to violate the rule for the scanner under test; the two test files need raw `posts` array shape access that the helper can't provide). Reverting them would land main in a worse state — chronic lint failure on changes that have been working in practice locally.
- The "Velite build (for Build 2)" CI step is on the boundary the reviewer noted: it is what makes the cadence step reachable in CI at all, and the cadence step lives in `ci.yml`. Recommend leaving that one step in this commit as in-scope-ish, but moving the eslint exemptions to a clean blog-core follow-up.
- Concrete recommendation: open `fix(blog-core): exempt intentional #site/content callsites from no-restricted-imports lint rule` as a separate task, then revert the eslint.config.mjs portion of df5aacb from main once that lands.

### Info — cadence step is not literally the CI workflow's "final step"

Acknowledged. The step is at `.github/workflows/ci.yml:135`, followed by Pagefind crawl/verify (5 steps), artifact upload, and the Vercel build/deploy chain. Functionally it gates deploy: the cadence step has `if: always()` and exits non-zero on trigger, the downstream Vercel deploy steps do not have `if: always()`, so a red cadence step prevents deploy. This is the intent of the restriction (cadence guard blocks rollout) even if the literal placement isn't the last step.

If the literal wording matters more than the functional intent, the step could be moved to live after all Vercel steps — flagging for the human reviewer to decide. Not changing position in this commit.

### Summary of fixes vs deferrals

| Finding | Severity | Status |
|---|---|---|
| Parser/doc ordering inversion | Critical | FIXED in this commit |
| Modulo gate silently skips | Warning | FIXED in this commit |
| Self-contradictory comment | Info | FIXED in this commit |
| CI runs green at launch | Warning | Deferred — recommend separate task for prettier drift |
| eslint.config.mjs scope creep | Warning | Deferred — recommend separate blog-core task + revert |
| Cadence step not literally final | Info | Acknowledged — functional intent preserved, awaiting human call on literal placement |

Per spec-workflow loop: STOPPING here. Trigger a re-review from the dashboard.

**Timestamp:** 2026-05-28T18:54:00.913Z
**Log ID:** 60e4c435-8a7a-4bad-b5b9-7bbdc73eee3a

---

## Statistics

- **Lines Added:** +18
- **Lines Removed:** -12
- **Files Changed:** 1
- **Net Change:** 6

## Files Modified
- scripts/check-lighthouse-cadence.mjs

## Files Created
_No files created_

---

## Artifacts

### Functions

#### check-lighthouse-cadence (review-v2 fixes)
- **Purpose:** Critical: parser now reads the LAST '- Published projects at run time: N' line in the runs-log via matchAll().at(-1), matching the doc's append-at-bottom ordering contract. Pre-fix, match() returned the first (launch) entry forever, so after the first cadence trigger lastCount stayed parsed at 0 and the guard could never be cleared. Post-fix verified: after appending Run 2 with count=3 at the bottom, a 3-published state correctly resolves to delta=0 and exits 0. Warning: dropped the redundant `currentCount % CADENCE_N === 0` clause from the trigger condition — batch-adds that bypassed a multiple (e.g. 4 published at once with lastCount=0) used to slip through; now `delta >= CADENCE_N` alone gates the trigger, faithful to Req 12.0's every-N-projects intent. Info: rewrote the previously self-contradictory comment block to state a single ordering invariant.
- **Location:** /home/mcf/repo/matthew-field.ca/scripts/check-lighthouse-cadence.mjs
- **Signature:** node scripts/check-lighthouse-cadence.mjs (no args)
- **Exported:** No

