# Implementation Log: Task 37

**Summary:** Documentation pass: fixed two drift items in design.md operator notes and added the GitHub UI repo-variable runbook subsection; created a minimal README.md with a one-line search affordance mention.

Drift fixes in design.md:
1. Vercel CLI version pinned (line ~166): replaced "exact-pinned" with "exact-pinned to `54.3.0`" to match `package.json` devDependencies.vercel = "54.3.0". Also pinned `pagefind` and `@pagefind/default-ui` to `1.5.2` to match actual.
2. Vercel API field shape (line ~766): replaced the prior `link.productionBranch` + `autoExposeSystemEnvs` placeholder text (which said "exact JSON field pinned during implementation") with the actually-implemented signal documented in `scripts/check-vercel-auto-deploy.mjs`: presence of top-level `project.link` object with a `link.type` string. Clarified that the v9 endpoint has no finer-grained auto-deploy toggle.

New subsection "Setting `PAGEFIND_ENABLED` and `DEPLOY_VIA_CI` repo variables" added under Operator notes (Req 13) immediately after the Repository variables table. Covers (1) Settings → Secrets and variables → Actions → Variables tab; (2) New repository variable; (3) names + default values (`PAGEFIND_ENABLED=true`; `DEPLOY_VIA_CI` UNSET until Req 0.3 v4 cutover); (4) ASCII text-diagram of resulting UI state.

README.md created (did not previously exist). Single line under "## Blog" mentions Pagefind search with `Cmd/Ctrl+K` and `/` shortcuts per Req 13.3. No full Search section; minimal Development block included only because the file was created from scratch.

Task 37 marked [-] in tasks.md per the spec-workflow convention; will be flipped to [x] only after the dashboard review per the user's spec-workflow loop preference.

**Timestamp:** 2026-05-23T03:20:24.223Z
**Log ID:** c8141478-3400-47d4-897c-4674acdc8e09

---

## Statistics

- **Lines Added:** +45
- **Lines Removed:** -3
- **Files Changed:** 3
- **Net Change:** 42

## Files Modified
- /home/mcf/repo/matthew-field.ca/.spec-workflow/specs/blog-enhanced/design.md
- /home/mcf/repo/matthew-field.ca/.spec-workflow/specs/blog-enhanced/tasks.md

## Files Created
- /home/mcf/repo/matthew-field.ca/README.md

---

## Artifacts

### Integrations

#### Integration
- **Description:** undefined
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

#### Integration
- **Description:** undefined
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

