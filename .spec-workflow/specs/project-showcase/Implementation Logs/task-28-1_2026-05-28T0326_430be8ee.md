# Implementation Log: Task 28.1

**Summary:** Dual-build local smoke + chokepoint negative + draft-warning count assertion all pass. Build 1 (PROJECTS_INCLUDE_DRAFTS=1): velite emits 1 warning (matching 1 draft entry in projects.json); next build OK; e2e for tasks 25/26/27 5 passed + 1 skipped (intentional empty-state). Build 2 (no env): glob of .next/server/app/projects for *fixture-placeholder* yields 0 matches (404 confirmed). Vitest Case 8 + Case 11 — 4/4 pass against real src/lib/projects.ts allowlist. Step 4 invariant: stderr_count==velite_draft_count==1.

**Timestamp:** 2026-05-28T03:26:52.137Z
**Log ID:** 430be8ee-033d-49a1-bd9a-937e7f55e4e1

---

## Statistics

- **Lines Added:** +1
- **Lines Removed:** -1
- **Files Changed:** 1
- **Net Change:** 0

## Files Modified
- /home/mcf/repo/matthew-field.ca/.spec-workflow/specs/project-showcase/tasks.md

## Files Created
_No files created_

---

## Artifacts

### Integrations

#### Integration
- **Description:** Verification run: dual-build local smoke + chokepoint negative + draft-warning count invariant. Step 1 (Build 1): PROJECTS_INCLUDE_DRAFTS=1 velite + next build EXIT=0; e2e --reporter=json results fragment: stats={expected:5,skipped:1,unexpected:0,flaky:0}; results=[{title:'wide-media escape math and a11y contracts hold at 1280×720',status:'passed',duration:713},{title:'draft-handling contract matches build flavor',status:'passed',duration:741},{title:'renders the expected published cards',status:'passed',duration:596},{title:'eager-loads the top-2 covers and lazy-loads the rest',status:'passed',duration:593},{title:'each card link\'s accessible name is its project title via aria-labelledby → <h3 id>',status:'passed',duration:555},{title:'renders the empty-state copy when no projects are published',status:'skipped',duration:181}]. The skipped spec is the empty-state guard (skips when projects exist) — intentional. Step 2 (Build 2): rm -rf .velite .next && pnpm exec velite build && pnpm build → EXIT=0. Glob command: `find /home/mcf/repo/matthew-field.ca/.next/server/app/projects -type f -name '*fixture-placeholder*'` → 0 matches (zero hits required, confirmed). For control, fixture-published-second.html/meta/rsc/segments DO appear under .next/server/app/projects. Step 3 (Chokepoint): `pnpm exec vitest run src/lib/projects.test.ts -t 'Case 8|Case 11' --reporter=json` → 4 passed / 0 failed: (a) 'runChokepointScan — canary kind coverage (Case 8) emits at least one finding in each expected kind group' PASS, (b) 'chokepoint allowlist self-test (Case 11) the production allowlist contains src/lib/projects.ts' PASS, (c) 'chokepoint allowlist self-test (Case 11) scanning src/lib/projects.ts under the production allowlist yields no violations' PASS, (d) 'chokepoint allowlist self-test (Case 11) a non-allowlisted file with a raw #site/content import IS a violation' PASS. Step 4 (Draft-warning count assertion): velite stderr captured during Build 1 contained the literal line `[velite/projects] PROJECTS_INCLUDE_DRAFTS=1 — including draft project: fixture-placeholder`. `grep -c '[velite/projects] PROJECTS_INCLUDE_DRAFTS=1 — including draft project:' /tmp/velite1.stderr` → 1. `node -e "console.log(require('./.velite/projects.json').filter(p => p.draft).length)"` (Build-1 .velite) → 1. Assertion: stderr_count (1) === velite_draft_count (1) — single-process Velite dedup invariant intact. Note: `pnpm build` in this project is `next build` only (velite is invoked separately via postinstall or by user; the test:e2e flow consumes `.velite/projects.json` produced by an explicit `pnpm exec velite build`). The draft-warning stderr was captured from the velite invocation that generated the Build-1 .velite snapshot consumed by the e2e suite, which is the substrate the assertion targets. No follow-ups filed — all four steps green.
- **Frontend Component:** n/a
- **Backend Endpoint:** n/a
- **Data Flow:** verification-only; no source code changes

