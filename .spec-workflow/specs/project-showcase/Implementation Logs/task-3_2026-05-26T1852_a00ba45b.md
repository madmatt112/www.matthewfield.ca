# Implementation Log: Task 3

**Summary:** Migrated src/lib/blog.ts to consume the shared formatter from src/lib/format-date.ts. Removed the inline `postDateFormatter` Intl.DateTimeFormat constant and the `formatPostDate` function literal, replacing both with `export const formatPostDate = formatContentDate;` (re-export form preserves reference identity for the Task 13 parity-triangle test).

**Timestamp:** 2026-05-26T18:52:20.017Z
**Log ID:** a00ba45b-d1a1-44fa-a723-922382577353

---

## Statistics

- **Lines Added:** +2
- **Lines Removed:** -8
- **Files Changed:** 1
- **Net Change:** -6

## Files Modified
- src/lib/blog.ts

## Files Created
_No files created_

---

## Artifacts

### Functions

#### formatPostDate
- **Purpose:** Blog-domain alias for the shared content date formatter. Defined as `export const formatPostDate = formatContentDate;` so `formatPostDate === formatContentDate` holds at runtime — required for the Task 13 parity-triangle reference-identity assertion. The old module-scope `postDateFormatter` Intl.DateTimeFormat instance has been removed; the single instance now lives in src/lib/format-date.ts and is shared with the future projects.ts (Task 10) alias.
- **Location:** src/lib/blog.ts:87
- **Signature:** export const formatPostDate: (iso: string) => { datetime: string; display: string }
- **Exported:** No

### Integrations

#### Integration
- **Description:** PostCard and other blog consumers continue to import `formatPostDate` from `@/lib/blog` unchanged. The export is now a const re-binding of `formatContentDate`, but the public signature `(iso: string) => { datetime: string; display: string }` is identical, so consumer call sites and types are unaffected. Typecheck passes; 69 blog tests pass.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

