# Implementation Log: Task 11

**Summary:** Implemented `runChokepointScan(filePath)` at `src/lib/build/check-projects-chokepoint.ts` per Component 11 v4. Uses the TypeScript compiler API (`ts.createSourceFile`) to walk the AST and detect all 17 coverage-matrix shapes of accidental `projects` import from `#site/content` (8 ES static + 2 dynamic + 1 type-only + 6 require variants). `isContentSpecifier` enforces exact equality on the specifier; sub-path imports out of scope per design. Pure function aside from `fs.readFileSync`; returns `ScanFinding[]` (empty on no findings).

**Timestamp:** 2026-05-27T01:46:23.342Z
**Log ID:** ed1da584-6cd1-4074-97d0-c0691ff272bf

---

## Statistics

- **Lines Added:** +263
- **Lines Removed:** -0
- **Files Changed:** 1
- **Net Change:** 263

## Files Modified
_No files modified_

## Files Created
- src/lib/build/check-projects-chokepoint.ts

---

## Artifacts

### Functions

#### runChokepointScan
- **Purpose:** AST-based scanner detecting the 17 coverage-matrix shapes of accidental `projects` import from `#site/content`. Reads the file, parses with the TypeScript compiler API, walks the AST in two passes (declarations + references), and returns one ScanFinding per detected shape. Pure function aside from the initial file read; returns empty array on no violations.
- **Location:** src/lib/build/check-projects-chokepoint.ts:108
- **Signature:** export function runChokepointScan(filePath: string): ScanFinding[]
- **Exported:** No

#### isContentSpecifier
- **Purpose:** Internal exact-equality check for the `#site/content` specifier per design policy. Sub-path imports (e.g. `#site/content/foo`) intentionally out of scope.
- **Location:** src/lib/build/check-projects-chokepoint.ts:79
- **Signature:** function isContentSpecifier(text: string): boolean
- **Exported:** No

#### isRequireCallOfContent
- **Purpose:** Internal helper: returns true for `require('#site/content')` calls with exact-equality specifier.
- **Location:** src/lib/build/check-projects-chokepoint.ts:97
- **Signature:** function isRequireCallOfContent(node: ts.CallExpression): boolean
- **Exported:** No

### Integrations

#### Integration
- **Description:** Scanner is consumed by `src/lib/projects.test.ts` (Task 14.2) to walk `src/**/*.{ts,tsx}` and assert no violations outside the allowlist (`src/lib/projects.ts`, `src/lib/projects.test.ts`, `src/__fixtures__/chokepoint-canary.ts`). Also exercised by the canary fixture (Task 12) to lock the 17-shape detection contract via positive assertions.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** filePath -> fs.readFileSync -> ts.createSourceFile -> AST walk -> ScanFinding[]

