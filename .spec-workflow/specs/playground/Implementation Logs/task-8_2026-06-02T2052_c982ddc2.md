# Implementation Log: Task 8

**Summary:** Resolved the two remaining Task 8 blockers. Blocker 1: changed the unlayered M1 .playground-container color from oklch(0.145 0 0) (which Chromium serializes as lab()) to the sRGB-hex equivalent #0a0a0a so getComputedStyle yields rgb(10, 10, 10), readable by the test's expectRgbEqual rgb-string parser (expectLabClose forbidden). Blocker 2: restructured ScribblePad so the six-hook fixture panel renders as a SIBLING of the serif-styled .root toy surface (wrapped both in a fragment) instead of a child, so the panel and its shadcn Button inherit .playground-container's re-established ui-sans-serif font instead of .root's Georgia serif. The toy surface keeps its clashing serif palette (Req 7.2); sample-plain-div keeps its inline serif+red; no data-testid, Tailwind class, or var() inline style changed. Rebuilt the prod bundle (the e2e webServer only runs `pnpm start` on the existing .next, so a fresh BLOG_INCLUDE_DRAFTS=1 build was required to pick up the CSS/TSX edits) then ran the isolation spec: all 7 assertions pass in both light and dark themes. EXPECTED_CONTAINER_COLOR_RGB confirmed/pinned at [10, 10, 10] (the exact passing serialized triple).

**Timestamp:** 2026-06-02T20:52:41.146Z
**Log ID:** c982ddc2-acc8-429b-b5cc-ada034d38962

---

## Statistics

- **Lines Added:** +14
- **Lines Removed:** -5
- **Files Changed:** 2
- **Net Change:** 9

## Files Modified
- /home/mcf/repo/matthew-field.ca/src/styles/playground.css
- /home/mcf/repo/matthew-field.ca/playground/scribble-pad/index.tsx

## Files Created
_No files created_

---

## Artifacts

### Components

#### ScribblePad
- **Type:** React
- **Purpose:** Same-page playground toy + six-hook CSS-isolation fixture. Restructured to render the data-testid fixture panel as a sibling of the serif .root toy surface so panel descendants inherit .playground-container's ui-sans-serif font.
- **Location:** /home/mcf/repo/matthew-field.ca/playground/scribble-pad/index.tsx
- **Props:** none (default export, no props)
- **Exports:** default

