---
id: c215915c-809b-4dc6-a556-5d2cac1cd9f7
taskId: "14"
specName: site-foundation
version: 1
verdict: findings
timestamp: 2026-04-17T17:20:03.512Z
criticalCount: 0
warningCount: 1
infoCount: 3
---

# Task Review: Task 14 (v1)

**Spec:** site-foundation
**Reviewed:** 2026-04-17T17:20:03.512Z
**Verdict:** findings

---

## Summary

Spike-results.md generally satisfies R11 AC7 with a clearly stated outcome (b), an overlay matrix, and contingency-aware guidance, but it contains a factual error about the current state of HOST_FONT_FAMILY_FRAGMENTS in the test file (claims Geist/Arial/Helvetica when the constant currently holds only Geist), a stale line reference, and a recommendation to spec 8 that has already been completed.

## Findings

### WARNING: Factual misstatement about HOST_FONT_FAMILY_FRAGMENTS contents — restriction violation
- **File:** .spec-workflow/specs/site-foundation/spike-results.md:57
- **Issue:** spike-results.md (line 57) states: "The `HOST_FONT_FAMILY_FRAGMENTS` negation list in `e2e/tests/playground-isolation.test.ts:93` currently contains `Geist`, `Arial`, `Helvetica`. The playground stack contains `Helvetica Neue` and `Arial`, so after the fix those two negations will fail as well — the reviewer called this out as a future-maintainability issue." The actual constant at e2e/tests/playground-isolation.test.ts:97 is `const HOST_FONT_FAMILY_FRAGMENTS = ["Geist"] as const;` — only one entry. The narrowing the document recommends ("narrow the negation list to fragments that cannot legitimately appear in the playground stack (just `Geist`)") has ALREADY been applied (likely in response to the v8 review of task 13, see memory-task-13.md line 26). This violates the task's explicit restriction "Be factual — report what was observed, not what was hoped" and the implementation summary's claim that line references and file paths were cross-checked against the current source tree. Spec 8 reading this will be misled about the present state of the test code, and the entire "Test-file fallout when M1 lands" subsection becomes load-bearing wrong: option (a) is a no-op rather than recommended action; the rationale ("future-maintainability issue") no longer applies. Fix by re-reading the file and rewriting the subsection to reflect that the list is already narrowed and only assertion (b) — invert to toContain — remains as future work.
- **Task Requirement:** Restriction: Be factual — report what was observed, not what was hoped

### INFO: Stale line reference for HOST_FONT_FAMILY_FRAGMENTS
- **File:** .spec-workflow/specs/site-foundation/spike-results.md:57
- **Issue:** The reference `e2e/tests/playground-isolation.test.ts:93` (spike-results.md line 57) points at a comment line. The constant declaration is on line 97. Even after the factual error is corrected, the line number should be updated to 97 so spec 8 lands on the actual constant rather than its preceding comment block.
- **Task Requirement:** R11 AC7 — actionable guidance for spec 8

### INFO: Layer-ordering behavior in Turbopack vs Webpack stated only as 'zero divergence', not as explicit ordering
- **File:** .spec-workflow/specs/site-foundation/spike-results.md
- **Issue:** The task prompt enumerates as item (2): "layer ordering behavior observed in Turbopack vs Webpack". The document addresses bundler parity (line 36: "15-property comparison table in `e2e/spike-summary.txt:137-153` shows zero divergence") but does not document the layer ordering itself — i.e., that `@layer playground` is declared in src/styles/globals.css:4 BEFORE `@import "tailwindcss"` so it sits below Tailwind's `base`/`components`/`utilities` layers in the cascade, and that this ordering is preserved by both bundlers. The empirical observation that bg-blue-500 / p-4 utilities apply (Tailwind's `utilities` layer outranking `playground`) is the load-bearing evidence; the document leaves the reader to infer the ordering from the absence of failure rather than stating it. Consider adding one sentence pointing to globals.css:4 and noting that in both bundlers, declared-first layers have lower priority and Tailwind utilities therefore win against playground-layered declarations on the same selector.
- **Task Requirement:** Task prompt item (2)

### INFO [hygiene]: Off-by-one in playground.css line-range citation
- **File:** .spec-workflow/specs/site-foundation/spike-results.md:46
- **Issue:** spike-results.md line 46 cites "lines 22-76" for the layered rule block in src/styles/playground.css. The block opens at line 22 (`@layer playground {`) but the closing brace of `@layer playground` is at line 77 — line 76 is the last `}` of the inner `.playground-container` rule. Either is defensible, but the more conventional citation would be 22-77 to enclose the whole `@layer` block. Minor accuracy nit.

