# Implementation Log: Task 21

**Summary:** Task 21 — Accessibility / contrast verification (axe, both themes). Added a new Playwright+axe test file that asserts ZERO color-contrast violations on the visual-design surfaces (landing /, profile /profile, blog post /blog/fixture-code, and a status-feedback StatusCallout-on-card state) in BOTH light and dark themes, filtering axe results to the color-contrast rule. To reach the status-feedback state without hydration, added a `status-callout` entry to the existing blog component-preview registry that server-renders all four StatusCallout tones (success/warning/info/error) nested inside a `bg-card` surface — the deepest legal nest per design §1 (status text on its /10 tint composited over card). The test also includes render-sanity assertions confirming the composite actually renders (this caught a stale reused dev server that was 404ing the new route). The brand focus ring (--ring = --brand, full alpha per design §1 ring-alpha note) is verified with a computed OKLCH→sRGB→WCAG contrast assertion against background/card/muted in both themes, asserting ≥3:1 non-text (WCAG 1.4.11), since axe color-contrast covers text not rings. typecheck passes; all 16 tests in the new file pass (8 axe contrast + 2 composite-render + 6 ring computed).

**Timestamp:** 2026-06-09T03:39:10.061Z
**Log ID:** f344347f-2f74-4f32-857e-c61e34029f1d

---

## Statistics

- **Lines Added:** +215
- **Lines Removed:** -0
- **Files Changed:** 3
- **Net Change:** 215

## Files Modified
- /home/mcf/repo/matthew-field.ca/src/app/(site)/blog/component-preview/registry/index.ts

## Files Created
- /home/mcf/repo/matthew-field.ca/e2e/tests/visual-design-axe.test.ts
- /home/mcf/repo/matthew-field.ca/src/app/(site)/blog/component-preview/registry/status-callout.tsx

---

## Artifacts

### Components

#### status-callout (component-preview registry entry)
- **Type:** React (server component)
- **Purpose:** Server-renders all four StatusCallout tones nested inside a bg-card surface (deepest legal nest per design §1) so axe can audit status-text-on-/10-tint-over-card contrast without hydration. Reachable at /blog/component-preview/status-callout when BLOG_INCLUDE_DRAFTS=1.
- **Location:** src/app/(site)/blog/component-preview/registry/status-callout.tsx
- **Exports:** default (RegistryEntry)

### Functions

#### oklchToSrgb / relLuminance / contrastRatio
- **Purpose:** In-test helpers computing OKLCH→sRGB→WCAG contrast ratio to verify the brand focus ring (full-alpha --brand) clears ≥3:1 non-text against background/card/muted in both themes per design §1 ring-alpha note (axe color-contrast does not cover focus rings).
- **Location:** e2e/tests/visual-design-axe.test.ts
- **Exported:** No

### Integrations

#### Integration
- **Description:** undefined
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

