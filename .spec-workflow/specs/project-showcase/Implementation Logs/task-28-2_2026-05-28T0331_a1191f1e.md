# Implementation Log: Task 28.2

**Summary:** Captured fixture-render screenshot for /projects/fixture-placeholder under Build 1 state (PROJECTS_INCLUDE_DRAFTS=1), at viewport 1280x720, fullPage:true, saved to docs/projects-showcase-fixture-render.png. PNG dimensions: 1280x2611 (width pinned to 1280 viewport; height extended by fullPage scroll capture). Build-state pin: Build 1 (route HTTP 200 confirms the draft fixture is reachable — would 404 in Build 2). Screenshot shows the wide-media substrate (wide img, wide table with Column A/B, wide pre fenced code, inline SVG with viewBox) visibly escaping wider than the narrow Overview paragraph in the prose column — empirical CSS-math evidence per Component 10 v4 implementation gate.

**Timestamp:** 2026-05-28T03:31:55.240Z
**Log ID:** a1191f1e-e948-4eb5-91a6-4e3cd6bb4d72

---

## Statistics

- **Lines Added:** +1
- **Lines Removed:** -1
- **Files Changed:** 2
- **Net Change:** 0

## Files Modified
- /home/mcf/repo/matthew-field.ca/.spec-workflow/specs/project-showcase/tasks.md

## Files Created
- /home/mcf/repo/matthew-field.ca/docs/projects-showcase-fixture-render.png

---

## Artifacts

### Integrations

#### Integration
- **Description:** Build 1 state ONLY (PROJECTS_INCLUDE_DRAFTS=1 pnpm exec velite build && PROJECTS_INCLUDE_DRAFTS=1 pnpm build) — explicitly NOT Build 2 since fixture-placeholder is draft:true and would 404 without the env flag. Build output confirmed `/projects/[slug]` SSG includes both /projects/fixture-placeholder AND /projects/fixture-published-second. Screenshot captured via a one-shot Node script using `@playwright/test` chromium against `pnpm start --port 3013`; the script navigates to http://127.0.0.1:3013/projects/fixture-placeholder (HTTP 200 verified), asserts viewport {width:1280,height:720}, and writes docs/projects-showcase-fixture-render.png via page.screenshot({fullPage:true}). Output PNG verified via `file` → `PNG image data, 1280 x 2611, 8-bit/color RGB, non-interlaced` (width=1280 pinned; height>720 expected with fullPage capture). The capture script was a temporary scaffold and was removed after the artefact was committed under docs/ (per the task pin: artefact under docs/, not a permanent script). Visual evidence: narrow Overview <p> is constrained to the prose column; wide <img>, wide GFM <table> (Column A/Column B + cell 1-4), fenced <pre> (Fixture type/const), inline SVG, and the cover image all visibly escape to the outer container's content width — confirming the anchored-escape CSS math at 1280×720.
- **Frontend Component:** n/a
- **Backend Endpoint:** n/a
- **Data Flow:** verification-only screenshot artefact; no source code changes (only docs/*.png added and tasks.md status flip)

