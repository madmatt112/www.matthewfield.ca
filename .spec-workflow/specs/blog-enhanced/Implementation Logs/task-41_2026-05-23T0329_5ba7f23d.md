# Implementation Log: Task 41

**Summary:** Added Playwright test that mechanically verifies the reading-progress bar honours `prefers-reduced-motion: reduce` (Req 5.5). Chose the NEW-file option (`e2e/tests/blog-reading-progress.test.ts`) over extending `blog-axe.test.ts` — clearer scope, isolates the reduced-motion concern from axe-core a11y scans, and matches the project convention of one-concern-per-test-file (cf. `contact-reduced-motion.test.ts`). The suite contains two cases: (1) WITH `page.emulateMedia({ reducedMotion: 'reduce' })` — computed `transition-duration` and `animation-duration` on `.reading-progress-fill` must both be 0s; (2) WITHOUT emulation (explicit `no-preference`) — at least one of those durations must be non-zero, guarding against an "always-zero" regression where someone hard-codes `transition: none`. Assertions read `window.getComputedStyle()` (not inline styles) per Task 41 restriction. Both navigate to `/blog/fixture-toc` and scroll halfway before reading the computed style. Typecheck clean; lint reports only pre-existing errors unrelated to this file.

**Timestamp:** 2026-05-23T03:29:05.273Z
**Log ID:** 5ba7f23d-ee9c-4fa4-9aff-aeaf2fd9033b

---

## Statistics

- **Lines Added:** +114
- **Lines Removed:** -1
- **Files Changed:** 2
- **Net Change:** 113

## Files Modified
- /home/mcf/repo/matthew-field.ca/.spec-workflow/specs/blog-enhanced/tasks.md

## Files Created
- /home/mcf/repo/matthew-field.ca/e2e/tests/blog-reading-progress.test.ts

---

## Artifacts

### Functions

#### parseDurationSeconds
- **Purpose:** Parse a CSS computed `transition-duration` / `animation-duration` value (which may be a comma-separated list like `100ms, 0s`) and return the MAX duration in seconds. Used to assert zero/non-zero motion durations on the reading-progress fill element.
- **Location:** /home/mcf/repo/matthew-field.ca/e2e/tests/blog-reading-progress.test.ts:27
- **Signature:** function parseDurationSeconds(value: string): number
- **Exported:** No

#### readMotionDurations
- **Purpose:** Locate the reading-progress fill element, read its computed `transitionDuration` and `animationDuration` via `window.getComputedStyle()` (NOT inline styles), and return both parsed to seconds.
- **Location:** /home/mcf/repo/matthew-field.ca/e2e/tests/blog-reading-progress.test.ts:43
- **Signature:** async function readMotionDurations(page: Page, selector: string): Promise<{ transitionSeconds: number; animationSeconds: number }>
- **Exported:** No

#### scrollHalfway
- **Purpose:** Scroll the page so the viewport sits roughly halfway through the article, then wait 50ms for the rAF-throttled scroll listener in `<ReadingProgress />` to paint. Ensures the fill element has a non-degenerate layout when computed styles are read.
- **Location:** /home/mcf/repo/matthew-field.ca/e2e/tests/blog-reading-progress.test.ts:64
- **Signature:** async function scrollHalfway(page: Page): Promise<void>
- **Exported:** No

### Integrations

#### Integration
- **Description:** undefined
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

