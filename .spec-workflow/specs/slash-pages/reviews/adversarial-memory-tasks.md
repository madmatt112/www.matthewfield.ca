# Adversarial Review Memory — tasks
Last updated: 2026-06-01 (after v3/r3 review — CONVERGED)

## Convergence status: CONVERGED / implementation-ready (r3)
r3 verified the r2 blocker re-fix with a **live repro of the exact CI condition** (UTC worker + mid-process TZ pin + dynamic import) — the guard now fires red on the un-fixed formatter in UTC CI. Task 5 inline is consistent; DAG/ordering/coverage intact. Only residual: one cosmetic Task 16 mis-statement (`THEME_STORAGE_KEY` is exported+imported by siblings, not a local copy idiom) — non-blocking, optional one-line fix. No fourth round warranted.

## Cumulative Findings Summary

### Verified RESOLVED in r3 (was the r2 blocker + nicks)
- **TZ-pinned regression guard (Task 2) inert in target file (r2 BLOCKER, Recurring) → RESOLVED, r3-VERIFIED.** v3 moved the guard to a dedicated NEW file `src/lib/format-date-tz.test.ts` with NO static format-date import, setting `process.env.TZ = "America/Toronto"` at the top before the lone dynamic `await import("@/lib/format-date")`. r3 attacked every escape hatch and confirmed the fix holds: (a) `format-date.ts` imports **nothing** (`:1-9`), so the dynamic import pulls in only the bare formatter — no blog/projects/`#site/content` chain; (b) `vitest.config.ts` has **no `setupFiles`** (only `globalSetup`, which spawns `pnpm velite build` in a subprocess and never imports format-date into the worker) and **no `isolate:false`** — clean per-file registry; (c) **the load-bearing premise — mid-process `process.env.TZ` reassignment affects subsequently-constructed `Intl.DateTimeFormat` even on a UTC-booted process — was reproduced live**: `TZ=UTC node`, ambient formatter → "May 29", post-reassign formatter → "May 28", dynamic-import-after-set → "May 28". The standing CI assertion (`display === "May 29, 2026"` under the pin) fails on any box if `timeZone:"UTC"` is reverted, so "verify once locally" is harness sanity, NOT the gate. The regression is now genuinely CI-guarded.
- **Task 5 dangling `SUBJECT_REL`/`subjectHeadings` + per-doc warning (r2, minor) → RESOLVED, r3-VERIFIED.** v3 defined `const SUBJECT_REL = "docs/slash-pages-authoring.md"` and `const subjectHeadings = SLASH_PAGES_HEADINGS` inline, required the production `::warning::` to interpolate the per-doc `rel`, and required the self-test to assert the warning text contains `SUBJECT_REL`. r3 confirmed the four CLI assertions are internally consistent with `main(AUTHORING_DOCS)` report-all semantics (neither-doc → two stderr lines + no warning; zero-byte subject → `warningCount === SLASH_PAGES_HEADINGS.length`, not the stale 9) and the two `rel` paths are not substring-confusable.
- **Task 16 `_Leverage` reuse-overstatement (r2, cosmetic) → PARTIALLY resolved v3; one slip introduced.** v3 reworded to "copy the local non-exported idiom (setupTheme/assertTheme/THEME_STORAGE_KEY/AXE_TAGS)". r3: three of those four ARE local non-exported (`AXE_TAGS`/`setupTheme`/`assertTheme`), but **`THEME_STORAGE_KEY` is `export const` in `theme-provider.tsx:6` and IMPORTED by both siblings (`blog-axe.test.ts:4`/`contact-axe.test.ts:4`)** — it should be imported, not copied. Cosmetic (the copied value `"theme"` still works); optional one-line fix. See r3 Novel finding.

### Novel in r3 (cosmetic, non-blocking)
- **Task 16 `THEME_STORAGE_KEY` mis-grouped as a local copy idiom (Novel, cosmetic).** It is exported and imported by the siblings; only `AXE_TAGS`/`setupTheme`/`assertTheme` are local. Fix: "import `THEME_STORAGE_KEY` from theme-provider; copy only the three local helpers/const." Does not block implementation.

### Accepted
- **UTC test was not a regression guard (r1, significant)**: `formatContentDate("2026-05-29T00:00:00.000Z") === "May 29, 2026"` passes on both the broken and fixed formatter when the CI runner's `TZ` is UTC. Resolved in v2 by pinning `process.env.TZ = "America/Toronto"` and dynamically importing `formatContentDate` after, so the module-const formatter constructs under that zone and the assertion goes red pre-fix. Supersedes the design's "no TZ harness / true by construction" framing.
- **Task 16 missing axe/WCAG pass (r1, significant)**: every sibling page-type (`blog-axe`, `contact-axe`, `projects-*`) runs `AxeBuilder` per page per theme; the five new slash pages had only render/console checks. Resolved in v2 by adding an `AxeBuilder().withTags(AXE_TAGS).analyze()` pass per page per theme to Task 16.
- **Task 5 deferred its hardest edits to design's literal diff spec (r1, significant)**: the four self-test edits (pure-core call-sites, `writeDocs` helper body, report-all control flow, per-doc zero-byte count) lived only in design.md. Resolved in v2 by inlining the full four-item checklist (incl. the `writeDocs` code block) into Task 5's body.
- **Task 6 Success under-covered colophon (r1, minor)**: Success line asserted about/now but not colophon body/stack content. Resolved in v2 by strengthening Task 6 Success and confirming Task 14 reads all three files.
- **Task 14 File line self-contradictory (r1, cosmetic)**: `src/config or src/lib` vs the `src/content/` example. Resolved in v2 by pinning a single concrete path with a note that any `src/**` location matches vitest include.

### Partially Accepted
- **Route-task Success lines proven only by Task 16 (r1, minor)**: no restructure needed; r1 itself said "none required if Task 16 stays." v2 added an explicit coupling note (route-tasks 7/9/11 are backstopped by Task 16/Task 3, not independently gated) rather than splitting the tasks. r2: coupling note verified accurate, contradicts no `_Depends on:` edge.
- **Task 5 inline (r2 on r1 Finding 3, minor, Compounding)**: v2 inlined the four-item checklist — substantially delivers self-containment — but two completions remain: edit (3) leaves `SUBJECT_REL`/`subjectHeadings`/`<subjectDoc>.headings.length` undefined inline (implementer must infer `= "docs/slash-pages-authoring.md"` / `= SLASH_PAGES_HEADINGS`), and the production `::warning::` message (`mjs:76`) hardcodes the contributions path but must become per-doc under `main(AUTHORING_DOCS)` (self-test only matches `/::warning::/`, won't catch a wrong path). Report-all control flow, the "neither doc → two stderr lines," and the zero-byte `=== subjectDoc.headings.length` (not the stale `9`) assertions are all internally consistent with the new `main()` contract. **Fix:** define the two identifiers inline + add a per-doc-message restriction.

### Rejected
- (none)

## Patterns & Themes
- r1's exposure was concentrated entirely in **`Success:`-gate rigor** and **one convention shortfall** (axe). r2 confirms the recurring tension — "true by construction"/"deliberate minimal harness" claims that *assert* a regression guard without one that actually fires — is the dominant failure mode: the v2 Task 2 fix *named* the right idea (a TZ harness) but the prescribed mechanism is inert because it ignored the target file's pre-existing static import + the no-`resetModules` constraint. **Lesson for future rounds: when a fix touches an existing test file, read that file's imports and header constraints — a fix can be defeated by the very file it edits.**
- Live-code grounding remains accurate for **static facts** (citations, line ranges, deps all verified again in r2), but r2 surfaced a class of *reuse-overstatement*: Task 16 `_Leverage` claims `AXE_TAGS`/`setupTheme`/`assertTheme` as importable from blog-axe, but all three are *local non-exported* consts/functions (`blog-axe.test.ts:24,26-49`; `contact-axe.test.ts:8`). The task *body* is correct ("local", hedge resolves to "mirror"), so cosmetic — but watch `_Leverage` lines for "reuse X" where X isn't exported.

## Guidance for Next Review
- **The document is CONVERGED after r3. No further adversarial round is warranted on technical grounds.** All r1/r2 blockers and nicks are resolved-and-verified; the only open item is one cosmetic Task 16 phrasing slip (`THEME_STORAGE_KEY`) that does not block a fresh-context implementer.
- If a fourth round is run anyway, do NOT re-litigate: the TZ guard (r3 proved it fires in UTC CI via live repro), Task 5 inline consistency, DAG/edges/ordering, axe scope (`@axe-core/playwright@^4.11.3` installed, no new dep/edge), the coupling note, AC 1.2 verify-only labelling, and every static citation — all re-verified through r3.
- Standing lesson (carried): when a fix touches an existing test file, read that file's imports/header constraints; a fix can be defeated by the very file it edits (the r2 blocker). r3 corollary: when claiming a symbol is "local non-exported, copy it," grep for `export` first — `THEME_STORAGE_KEY` was an export the siblings import, not a local idiom.
